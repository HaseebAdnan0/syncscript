## Iteration 1 - US-001: Project Initialization & Core Dependencies
- Installed missing core dependencies (@tanstack/react-query, class-variance-authority, clsx, tailwind-merge, lucide-react, next-themes)
- Installed Radix UI primitives (dialog, dropdown-menu, tabs, toast)
- Installed prettier as dev dependency
- Verified folder structure exists (components/ui, components/features, lib/, hooks/, styles/)
- Verified .env.local.example and .gitignore are properly configured
- Confirmed npm run dev starts without errors
- Confirmed typecheck passes
- Files changed: package.json, package-lock.json, PRD4.md
- Learnings for future iterations:
  - Frontend is a git submodule - must commit within frontend/ directory first, then update parent repo
  - The project was partially initialized already, so mainly needed to install missing dependencies
  - All folder structure was already in place
  - npm run type-check script already exists in package.json
---

## Iteration 2 - US-002: TypeScript Strict Configuration
- Updated tsconfig.json with strict compiler options
- Added target: ES2020, noUnusedLocals, noUnusedParameters, noImplicitReturns
- Added allowSyntheticDefaultImports, forceConsistentCasingInFileNames
- Verified npm run type-check passes with strict settings
- Files changed: frontend/tsconfig.json, PRD4.md
- Learnings for future iterations:
  - PRD4.md checkboxes may already be updated from previous backend work
  - Frontend submodule pointer is automatically updated when committing in parent repo
  - TypeScript strict mode caught no issues because the project was freshly scaffolded
---
## Iteration 3 - US-003: Tailwind CSS Design System Configuration
- Updated tailwind.config.ts with Bitcoin DeFi design tokens (colors, fontFamily, boxShadow)
- Replaced local Geist fonts with Google Fonts (Space Grotesk, Inter, JetBrains Mono) in layout.tsx
- Updated globals.css with custom utilities: bg-grid-pattern, glass-card, glass-input, glass-surface
- Added animate-float keyframe animation for hero elements
- Updated app metadata to SyncScript branding
- Verified typecheck passes and dev server starts without errors
- Files changed: frontend/tailwind.config.ts, frontend/src/app/layout.tsx, frontend/src/app/globals.css, PRD4.md
- Learnings for future iterations:
  - Google Fonts automatically handle variable font CSS variables when using next/font/google
  - Tailwind v4 still uses extend pattern in config, not @theme directive (that's for pure CSS config)
  - Custom utilities should be in @layer components for complex patterns, @layer utilities for simple ones
  - Dev server starts quickly with Next.js 14.2.35 (~2.4s)
---

## Iteration 4 - US-004: ShadCN UI Initialization
- Ran `npx shadcn@latest init --defaults` to initialize ShadCN UI
- Manually updated components.json from "new-york" to "default" style to match PRD requirements
- Created src/lib/utils.ts with cn() helper function for className merging
- ShadCN init automatically installed tailwindcss-animate dependency
- ShadCN init automatically added CSS variables to globals.css
- Verified typecheck passes
- Files changed: frontend/components.json, frontend/src/lib/utils.ts, frontend/package.json, frontend/src/app/globals.css, PRD4.md
- Learnings for future iterations:
  - ShadCN init with --defaults flag uses "new-york" style by default, not "default" - need to manually update components.json
  - The init command sets up all aliases correctly in components.json (@/components, @/lib/utils, etc.)
  - CSS variables for colors are auto-injected into globals.css by ShadCN
  - tailwindcss-animate is required for ShadCN components and auto-installed
---

## Iteration 5 - US-005: Base Component Library (Button, Card, Input, Badge)
- Created Button component with CVA variants (primary, outline, ghost) and sizes (sm, md, lg)
- Created Card component with six subcomponents (Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter)
- Created Input component with bottom-border focus style
- Created Badge component with four variants (default, success, warning, error)
- All components use Bitcoin DeFi design tokens (primary, secondary, accent, surface, etc.)
- All components use cn() utility for className merging
- Verified typecheck passes
- Files changed: frontend/src/components/ui/button.tsx, frontend/src/components/ui/card.tsx, frontend/src/components/ui/input.tsx, frontend/src/components/ui/badge.tsx, PRD4.md
- Learnings for future iterations:
  - CVA (class-variance-authority) allows clean variant management for components
  - Button uses forwardRef pattern for ref forwarding to underlying HTML element
  - Badge uses regular function component (no forwardRef needed for div wrapper)
  - Card components export all six subcomponents for flexible composition
  - Input extends React.InputHTMLAttributes<HTMLInputElement> for full HTML props
  - All color tokens from tailwind.config.ts work seamlessly (primary, secondary, accent, surface, etc.)
---

## Iteration 6 - US-006: Layout & Theme Infrastructure
- Created layout.tsx with three layout components (Container, Section, PageLayout)
- Created theme-provider.tsx with forced dark mode using next-themes
- Updated root layout.tsx to import and wrap children in ThemeProvider
- All components properly export TypeScript types (ContainerProps, SectionProps, PageLayoutProps)
- Container uses max-w-7xl mx-auto with responsive padding
- Section uses py-24 spacing
- PageLayout provides flexible layout with optional header/footer slots
- Verified typecheck passes and dev server starts successfully
- Files changed: frontend/src/components/layout.tsx, frontend/src/components/theme-provider.tsx, frontend/src/app/layout.tsx, PRD4.md
- Learnings for future iterations:
  - next-themes ThemeProvider needs 'use client' directive since it uses React context
  - forcedTheme="dark" prevents theme toggle and locks app to dark mode
  - attribute="class" is needed for Tailwind dark mode to work properly
  - Layout components should accept className prop for flexibility
  - PageLayout uses flex-col and flex-1 for sticky footer pattern
  - Font CSS variables were already set up in previous iteration
---

## Iteration 7 - US-007: API Client with JWT Auto-Refresh
- Created src/lib/types/api.ts with common API response types
- Added PaginatedResponse, ApiErrorResponse, SuccessResponse interfaces
- Added TokenResponse and LoginResponse for auth endpoints
- API client (api.ts) was already implemented with JWT auto-refresh in previous work
- Verified typecheck passes
- Files changed: frontend/src/lib/types/api.ts
- Learnings for future iterations:
  - api.ts was created earlier and already has full JWT auto-refresh logic
  - The api.ts references @/stores/auth which will be created in US-008
  - Frontend is NOT a git submodule - it's a standalone repo in frontend/ directory
  - Must commit frontend changes separately from parent repo
  - Common API types should include generic types like PaginatedResponse for reusability
---

## Iteration 8 - US-008: Auth Store with Zustand
- Created src/lib/types/user.ts with User interface
- Created src/lib/stores/auth.ts with Zustand persist middleware
- Implemented AuthState interface with user, accessToken, refreshToken, isAuthenticated
- Added three actions: login(), logout(), setAccessToken()
- Used skipHydration: true to avoid SSR hydration mismatches
- Verified typecheck passes
- Files changed: frontend/src/lib/stores/auth.ts, frontend/src/lib/types/user.ts, PRD4.md
- Learnings for future iterations:
  - Zustand persist middleware requires 'use client' directive since it uses browser localStorage
  - skipHydration: true is critical for SSR apps to prevent hydration mismatch errors
  - The persist middleware wraps the create() call with persist((set) => {...}, { name, skipHydration })
  - Auth store is now available for api.ts to reference via @/stores/auth alias
  - User type includes optional fields (first_name, last_name) for Django user model compatibility
---

## Iteration 9 - US-009: React Query Provider
- Created src/lib/providers/query-provider.tsx with QueryClientProvider
- Configured QueryClient with sensible defaults: retry: 1, staleTime: 5min, refetchOnWindowFocus: false
- Added 'use client' directive for client-side rendering
- Imported and wrapped children in QueryProvider inside root layout.tsx
- Verified typecheck passes
- Files changed: frontend/src/lib/providers/query-provider.tsx, frontend/src/app/layout.tsx, PRD4.md
- Learnings for future iterations:
  - QueryClient should be instantiated inside a useState hook to avoid re-creating on every render
  - React Query provider needs 'use client' directive since it uses React context
  - Provider must be nested inside ThemeProvider in root layout
  - QueryClient default options apply to all queries unless overridden
---

