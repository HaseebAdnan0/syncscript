# PRD8 Progress Log

## Iteration 1 - US-001: Vault TypeScript types and API client
- Created `frontend/src/lib/types/vault.ts` with comprehensive TypeScript types:
  - VaultRole enum (OWNER, CONTRIBUTOR, VIEWER)
  - Vault interface with all necessary fields
  - VaultMember interface
  - Request/Response types for API operations
  - List response types with pagination support
- Created `frontend/src/lib/api/vaults.ts` with all vault API functions:
  - CRUD operations: getVaults, getVault, createVault, updateVault, deleteVault
  - Archive operation: archiveVault
  - Member management: getVaultMembers, addVaultMember, inviteVaultMember, updateMemberRole, removeMember
- All functions use existing axios instance from `frontend/src/lib/api.ts`
- Typecheck passes successfully

Files changed:
- frontend/src/lib/types/vault.ts (created)
- frontend/src/lib/api/vaults.ts (created)
- PRD8.md (marked US-001 complete)

Learnings for future iterations:
- The existing API client is in `frontend/src/lib/api.ts` (not `client.ts` as PRD mentioned)
- The axios instance is exported as `api` from this file
- The API has interceptors for auth token handling and refresh logic
- TypeScript compilation is fast and reliable for verification
---

## Iteration 2 - US-002: React Query hooks for vaults
- Created `frontend/src/hooks/useVaults.ts` with comprehensive React Query hooks:
  - useVaults(search?: string) - Fetches all vaults with optional search filtering
  - useVault(id) - Fetches single vault by ID
  - useCreateVault() - Creates new vault with automatic cache invalidation
  - useUpdateVault(id) - Updates vault and refreshes cache
  - useDeleteVault() - Deletes vault and removes from cache
  - useArchiveVault(id) - Archives vault (soft delete) with cache update
- Implemented query key factory pattern (vaultKeys) for organized cache management
- All mutations properly invalidate relevant queries on success
- useUpdateVault and useArchiveVault use setQueryData for optimistic updates
- Fixed pre-existing typecheck error in useAuth.ts (removed unused LoginCredentials interface)
- Typecheck passes successfully

Files changed:
- frontend/src/hooks/useVaults.ts (created)
- frontend/src/hooks/useAuth.ts (fixed unused import)
- PRD8.md (marked US-002 complete)

Learnings for future iterations:
- React Query provider already set up in `frontend/src/lib/providers/query-provider.tsx`
- Query key factory pattern keeps cache management organized and type-safe
- The `@/` import alias is configured and used throughout the project
- Mutations should invalidate lists() and update individual detail() queries
- enabled: !!id pattern prevents queries from running with falsy IDs
---

## Iteration 3 - US-003: Vault members API client and hooks
- Created `frontend/src/hooks/useVaultMembers.ts` with comprehensive member management hooks:
  - useVaultMembers(vaultId) - Fetches all members of a vault
  - useAddMember(vaultId) - Adds existing user by email/username
  - useInviteMember(vaultId) - Sends email invitation to new user
  - useUpdateRole(vaultId) - Updates member's role (accepts { memberId, data })
  - useRemoveMember(vaultId) - Removes member from vault (accepts memberId)
- Implemented query key factory pattern (vaultMemberKeys) for cache management
- All mutations properly invalidate members list query on success
- API functions already existed from US-001 (created in Iteration 1)
- Typecheck passes successfully

Files changed:
- frontend/src/hooks/useVaultMembers.ts (created)
- PRD8.md (marked US-003 complete)

Learnings for future iterations:
- Member management hooks follow same pattern as vault hooks (query key factory + invalidation)
- Some mutations need compound parameters (e.g., useUpdateRole takes { memberId, data })
- Hooks should be vault-scoped (take vaultId parameter) for member operations
- API client was already comprehensive from US-001, just needed React Query wrappers
---

## Iteration 4 - US-004: Zustand store for vaults UI state
- Created `frontend/src/stores/vaultsStore.ts` with Zustand store for global UI state:
  - isCreateModalOpen: boolean - Controls create vault modal visibility
  - searchQuery: string - Stores vault search filter text
  - activeTab: string - Persists active tab on vault detail page (defaults to 'sources')
  - Actions: openCreateModal, closeCreateModal, setSearchQuery, setActiveTab
- Store follows minimal state principle - only truly global UI state
- Typecheck passes successfully

Files changed:
- frontend/src/stores/vaultsStore.ts (created)
- PRD8.md (marked US-004 complete)

Learnings for future iterations:
- Zustand create() pattern is simple: define interface + create store with initial state + actions
- Keep stores minimal - component-local state should stay in useState/useReducer
- Default activeTab to 'sources' since that's typically the first tab users want
- Next.js requires building the app before typecheck to generate .next/types files
---

## Iteration 5 - US-005: VaultCard component
- Created `frontend/src/components/features/vaults/VaultCard.tsx` with complete vault card UI:
  - Displays vault name, description (truncated to 100 chars), source count, member count, last updated
  - Role badges with custom colors: Owner (orange), Contributor (blue), Viewer (gray)
  - Bitcoin DeFi styling: `bg-[#0F1115] border border-white/10 rounded-2xl`
  - Hover effects: `-translate-y-1` lift and `border-primary/50` orange glow
  - Card wraps in Link component for navigation to `/vaults/[id]`
  - Helper functions: getRoleBadgeStyles() for badge colors, formatDate() for human-readable timestamps
  - Uses Lucide icons: FileText (sources), Users (members), Clock (last updated)
- Typecheck passes successfully

Files changed:
- frontend/src/components/features/vaults/VaultCard.tsx (created)
- PRD8.md (marked US-005 complete)

Learnings for future iterations:
- UI component imports must use lowercase: `@/components/ui/card` and `@/components/ui/badge` (not Card/Badge)
- The existing Card component from shadcn already has hover effects built-in (from iteration 4 context)
- Badge component uses CVA for variants, but can override with className for custom colors
- formatDate helper creates human-readable relative timestamps (Today, Yesterday, X days ago, etc.)
- Link from next/link should wrap the Card for proper navigation and accessibility
- line-clamp-2 utility class for multi-line text truncation
---

