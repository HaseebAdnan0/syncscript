# Progress Log

## Feature: Frontend Sources & Annotations UI (PRD9)

## Learnings
(Patterns discovered during implementation)

---

## Iteration 1 - US-001: Create Source TypeScript types and API client
- Created `frontend/src/lib/types/sources.ts` with Source, SourceType, SourceMetadata types
- Created `frontend/src/lib/types/annotations.ts` with Annotation, AnnotationReply types
- Created `frontend/src/lib/api/sources.ts` with CRUD functions (getSources, getSource, createSource, updateSource, deleteSource)
- Created `frontend/src/lib/api/annotations.ts` with CRUD functions (getAnnotations, createAnnotation, createReply, deleteAnnotation)
- All API functions use axios instance from existing lib/api setup
- Typecheck passes ✓

Files changed:
- frontend/src/lib/types/sources.ts (new)
- frontend/src/lib/types/annotations.ts (new)
- frontend/src/lib/api/sources.ts (new)
- frontend/src/lib/api/annotations.ts (new)
- PRD9.md (marked US-001 complete)

Learnings for future iterations:
- TypeScript types follow existing pattern from lib/types/api.ts with PaginatedResponse generic
- API clients use axios instance from lib/api.ts with built-in error handling via handleApiError
- Sources API returns paginated results, so getSources extracts response.data.results
- Annotations include nested replies array in the response structure
- All CRUD operations follow RESTful URL patterns: /vaults/{id}/sources/, /sources/{id}/annotations/
- Files were committed in frontend submodule commit d89e7bf alongside vault types/API
---

## Implementation Notes

### Dependencies to verify before starting:
- [ ] Backend Sources API endpoints exist and return expected shape
- [ ] Backend Annotations API endpoints exist with reply support
- [ ] WebSocket channel includes source/annotation events
- [ ] Vault detail page shell exists at /vaults/[id]

### Design tokens reference:
- Background: #030304 (True Void)
- Surface: #0F1115 (Dark Matter)
- Muted text: #94A3B8 (Stardust)
- Primary: #F7931A (Bitcoin Orange)
- Glass morphism: backdrop-blur-lg bg-white/5 border border-white/10

---
## Iteration 2 - US-002: Create SourceCard component
- Created SourceCard component at frontend/src/components/features/sources/SourceCard.tsx
- Implemented glass morphism styling: bg-[#0F1115] border border-white/10 rounded-2xl
- Displays source title, type badge (using default Badge variant), contributor avatar (gradient circle), and created date
- Shows truncated URL or citation preview (80 char limit with ellipsis)
- Hover effect: -translate-y-1 and border-[#F7931A]/50 with transition-all duration-300
- Links to /vaults/{vaultId}/sources/{source.id} for detail page navigation
- Installed date-fns package for date formatting (format function)
- Typecheck passes ✓

Files changed:
- frontend/src/components/features/sources/SourceCard.tsx (new)
- frontend/package.json (added date-fns dependency)
- frontend/package-lock.json (updated with date-fns)
- PRD9.md (marked US-002 complete except browser verification)

Learnings for future iterations:
- Source type uses camelCase properties: source.type, source.createdAt (not snake_case)
- Badge component supports variants: default, success, warning, error (no outline variant)
- date-fns format() function used for date display: format(new Date(source.createdAt), 'MMM d, yyyy')
- Contributor avatar rendered as gradient circle with first letter of username
- Muted text color is #94A3B8 (Stardust) from design tokens
- Glass morphism cards use specific hover transitions matching design system
- SourceMetadata can have unknown keys via index signature, need type assertion for citation
---

## Iteration 3 - US-003: SourceTypeBadge already implemented
- US-003 (SourceTypeBadge component) was already completed in a previous iteration
- Component exists at frontend/src/components/features/sources/SourceTypeBadge.tsx
- PRD9.md already correctly marked as complete
- No new work needed - moving to next task

Learnings for future iterations:
- Always check git history before implementing - avoid duplicate work
- SourceTypeBadge uses lucide-react icons: Globe (URL), FileText (PDF), Quote (Citation), Newspaper (Article)
- Badge colors use semantic mapping: blue=URL, red=PDF, purple=Citation, green=Article
- Badge styling follows pattern: bg-{color}/20 text-{color} border-{color}/30
---

## Iteration 4 - US-004: Create SourceTableRow component
- Created dropdown-menu UI component at frontend/src/components/ui/dropdown-menu.tsx
- Created SourceTableRow component at frontend/src/components/features/sources/SourceTableRow.tsx
- Implemented table row with: type badge, linked title, contributor, date added, actions dropdown
- Row hover effect: bg-white/5 transition-colors
- Actions dropdown uses Radix UI @radix-ui/react-dropdown-menu (already installed)
- Dropdown includes: View (link to detail), Edit (callback), Delete (callback with red styling)
- Delete option separated with DropdownMenuSeparator and styled with text-red-400
- Typecheck passes ✓ (no errors in new files)

Files changed:
- frontend/src/components/ui/dropdown-menu.tsx (new)
- frontend/src/components/features/sources/SourceTableRow.tsx (new)
- PRD9.md (marked US-004 complete except browser verification)

Learnings for future iterations:
- Radix dropdown menu uses Portal for proper z-index stacking
- Glass morphism dropdown: bg-[#0F1115] border border-white/10 backdrop-blur-lg
- DropdownMenuItem supports asChild prop for Link components
- Table row transitions use hover:bg-white/5 for subtle highlight
- Actions menu icon uses MoreVertical from lucide-react
- Destructive actions (delete) use text-red-400 focus:text-red-400
- Optional callbacks (onEdit, onDelete) allow parent to control functionality
---

## Iteration 5 - US-005 & US-006: SourcesListHeader and view preference hook
- Created SourcesListHeader component at frontend/src/components/features/sources/SourcesListHeader.tsx
- Created useSourcesViewPreference hook at frontend/src/hooks/useSourcesViewPreference.ts
- Header displays "Sources" title with count badge (orange Bitcoin theme)
- View toggle buttons (Grid/List icons) with active state using orange gradient
- Active button has gradient bg: from-[#EA580C] to-[#F7931A] with orange shadow
- Inactive buttons show muted color (#94A3B8) with hover to white
- Add Source button uses primary gradient style with rounded-full shape
- Hook manages localStorage persistence with key 'syncscript:sources-view'
- Default view mode is 'grid', persists immediately on change
- Typecheck passes ✓

Files changed:
- frontend/src/components/features/sources/SourcesListHeader.tsx (new)
- frontend/src/hooks/useSourcesViewPreference.ts (new)
- PRD9.md (marked US-005 and US-006 complete except browser verification)

Learnings for future iterations:
- View toggle uses button elements (not Button component) for tight control over styling
- Active state uses same gradient as primary buttons for consistency
- localStorage requires useEffect for SSR-safe initialization in Next.js
- Badge count uses Bitcoin orange theme: bg-[#F7931A]/20 text-[#F7931A] border-[#F7931A]/30
- Toggle buttons grouped in glass morphism container: bg-[#0F1115] border border-white/10 rounded-lg
- Icons from lucide-react: Grid and List for view modes, Plus for add button
- Files were committed in commit 610352e alongside empty vaults state
---

## Iteration 6 - US-007: SourcesFilterBar already implemented
- US-007 (SourcesFilterBar component) was already completed in a previous iteration
- Component exists at frontend/src/components/features/sources/SourcesFilterBar.tsx
- Fixed TypeScript errors in unrelated file (useNotifications.ts) to allow typecheck to pass
- PRD9.md already correctly marked tasks as complete
- Typecheck passes ✓

Files changed:
- frontend/src/hooks/useNotifications.ts (fixed type annotations for callback parameters)
- PRD9.md (already marked US-007 complete except browser verification)

Learnings for future iterations:
- SourcesFilterBar manages filter state via URL search params using useSearchParams/useRouter
- Filter buttons use glass morphism style: bg-[#0F1115] border border-white/10 backdrop-blur-lg
- Active filters show with bg-[#F7931A]/20 text-[#F7931A] highlighting
- Clear filters button appears conditionally with red styling (bg-red-500/10 border border-red-500/30)
- URL params ensure filters are shareable and persist across page refreshes
- Contributor filter populated from vaultMembers prop array
- Date options: Any Time, Today, This Week, This Month, Custom Range
- Type options: All Types, URL, PDF, Citation, Article
- When fixing TypeScript errors in React Query mutations, need explicit type annotations for callback parameters
---
---

## Iteration 7 - US-008: Create SourcesList container component
- Created SourcesList component at frontend/src/components/features/sources/SourcesList.tsx
- Implemented grid view with CSS grid: grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6
- Implemented table view with sticky header (bg-black/50 border-b border-white/10 sticky top-0)
- Table wrapped in overflow-x-auto for horizontal scrolling on small screens
- Created SourceCardSkeleton for grid loading state (6 cards shown)
- Created SourceTableRowSkeleton for table loading state (10 rows shown)
- Both skeletons use animate-pulse with bg-white/10
- Empty state uses FileQuestion icon in gradient circle with 20% opacity
- Empty state CTA button uses full gradient button style with shadow
- Component accepts viewMode prop and conditionally renders grid or table
- Also accepts optional callbacks: onAddSource, onEdit, onDelete
- Fixed unrelated TypeScript errors:
  - VaultToast.tsx: Changed annotation.user to annotation.author (already auto-fixed)
  - login/page.tsx: Changed named imports to default imports for GradientButton and GlassCard (already auto-fixed)
- Typecheck passes ✓

Files changed:
- frontend/src/components/features/sources/SourcesList.tsx (new)
- frontend/src/app/(auth)/login/page.tsx (created - was missing)
- frontend/src/components/features/notifications/VaultToast.tsx (fixed author property)
- PRD9.md (marked US-008 complete except browser verification)

Learnings for future iterations:
- Loading skeletons should match the structure of actual content (same grid/table layout)
- Empty states should be encouraging and include clear CTAs for first-time users
- Empty state icons should use gradient circles with reduced opacity to maintain design system
- Table sticky headers need explicit bg color (bg-black/50) to remain visible while scrolling
- Skeleton counts: 6 for grid cards, 10 for table rows (good UX balance)
- FileQuestion icon from lucide-react is perfect for empty source states
- Component design allows parent to control all actions via optional callbacks
- Always fix unrelated TypeScript errors to ensure typecheck passes for current task
---

## Iteration 8 - US-009: Create useSourcesQuery hook (already completed)
- US-009 (useSourcesQuery hook) was already implemented in a previous iteration
- Hook exists at frontend/src/hooks/useSourcesQuery.ts with correct implementation
- Accepts vaultId and optional filters parameter
- Uses React Query with queryKey: ['sources', vaultId, filters]
- Returns data, isLoading, error, refetch (standard React Query return)
- Stale time correctly set to 30 seconds (30 * 1000)
- Created missing Textarea UI component (frontend/src/components/ui/textarea.tsx)
- Textarea follows design system: bg-black/50, border-white/20, focus:border-[#F7931A]
- Fixed imports in CreateVaultModal (already auto-fixed)
- All changes already committed in previous workflow
- Typecheck passes ✓
- PRD9.md already correctly marked US-009 as complete

Files changed:
- frontend/src/hooks/useSourcesQuery.ts (already existed)
- frontend/src/components/ui/textarea.tsx (created to fix missing dependency)
- PRD9.md (already marked US-009 complete)

Learnings for future iterations:
- useSourcesQuery follows same pattern as useAnnotations hook
- Query key includes filters to ensure cache invalidation when filters change
- enabled: !!vaultId prevents query from running if vaultId is not provided
- Textarea component needed for CreateVaultModal - uses same bottom-border focus style
- cn() utility function used for className merging in UI components
- React.forwardRef pattern used for all UI components to support ref forwarding
---

## Iteration 9 - US-010: Create AddSourceModal component
- Created Dialog UI component at frontend/src/components/ui/dialog.tsx
- Dialog follows Radix UI pattern with glass morphism styling
- DialogContent uses bg-[#0F1115] border border-white/10 backdrop-blur-lg for glass effect
- DialogOverlay uses bg-black/80 backdrop-blur-sm for darkened backdrop
- Close button (X) positioned absolute top-right with orange focus ring
- Created AddSourceModal at frontend/src/components/features/sources/AddSourceModal.tsx
- Modal accepts open, onOpenChange, and vaultId props
- URL input uses bottom-border focus style: border-b-2 border-white/20 focus:border-[#F7931A]
- Fetch Metadata button uses full gradient style with loading state (Loader2 icon + spinner)
- Loading state shows spinner with "Fetching metadata from URL..." message
- Enter key triggers metadata fetch for better UX
- Button disabled when URL is empty or while loading
- Typecheck passes ✓

Files changed:
- frontend/src/components/ui/dialog.tsx (new)
- frontend/src/components/features/sources/AddSourceModal.tsx (new)
- PRD9.md (marked US-010 complete except browser verification)

Learnings for future iterations:
- Dialog component follows same export pattern as dropdown-menu (individual exports for each part)
- Glass morphism modal uses rounded-2xl border with backdrop-blur-lg
- DialogOverlay creates the darkened backdrop automatically via Portal
- Input with Enter key handler improves UX for form submission
- Loader2 icon from lucide-react with animate-spin for loading states
- Button gradient uses shadow-[0_0_20px_-5px_rgba(234,88,12,0.5)] for orange glow effect
- Modal dialogs use DialogHeader with DialogTitle and DialogDescription for accessibility
- vaultId will be used in next user story for actual metadata fetching/source creation
---

## Iteration 10 - US-011: Create SourceMetadataPreview component
- Created SourceMetadataPreview component at frontend/src/components/features/sources/SourceMetadataPreview.tsx
- Component displays metadata preview in glass morphism card: bg-[#0F1115] border border-white/10 rounded-xl
- Shows favicon (or Globe icon fallback), detected source type badge, editable title input, and description
- Title input uses bottom-border focus style: border-b-2 border-white/20 focus:border-[#F7931A]
- Editable title managed via local state (useState) - allows user to customize before adding
- "Add Source" button uses full gradient style with disabled state when title is empty or loading
- "Cancel" button uses secondary glass morphism style: bg-[#0F1115] border border-white/20
- Error state shows red-themed alert box with retry and cancel buttons
- Error box uses bg-red-500/10 border border-red-500/30 for consistency with error styling
- Retry button conditionally rendered when onRetry callback provided
- All buttons follow hover transitions from design system
- Typecheck passes ✓

Files changed:
- frontend/src/components/features/sources/SourceMetadataPreview.tsx (new)
- PRD9.md (marked US-011 complete except browser verification)

Learnings for future iterations:
- Metadata preview needs editable title field so users can customize before saving
- Favicon fallback uses Globe icon in bg-white/10 circle to match design system
- Error states should provide retry option via optional callback pattern
- Description text uses white/80 opacity for subtle hierarchy vs title
- Label text uses Stardust color (#94A3B8) for consistent muted metadata
- Disabled button state needs opacity-50 + cursor-not-allowed + no hover scale
- Component accepts callbacks (onAddSource, onCancel, onRetry) for parent control
- detectedType from metadata determines which SourceTypeBadge variant to show
---
## Iteration 11 - US-012: Create useAddSourceMutation hook
- Created useAddSourceMutation hook at frontend/src/hooks/useAddSourceMutation.ts
- Follows same mutation pattern as useCreateVault from useVaults.ts
- Uses React Query useMutation with Source, Error, CreateSourceRequest generic types
- Invalidates sources query cache using queryKey: ['sources', vaultId] on success
- Shows success toast with source title confirmation message
- Shows error toast on failure with error message
- Fixed unrelated TypeScript error: removed unused SourcesList import in vault detail page
- Typecheck passes ✓

Files changed:
- frontend/src/hooks/useAddSourceMutation.ts (new)
- frontend/src/app/vaults/[id]/page.tsx (fixed unused import)
- PRD9.md (marked US-012 complete)

Learnings for future iterations:
- Mutation hooks follow pattern: useMutation<ReturnType, ErrorType, RequestType>
- Query invalidation uses queryClient.invalidateQueries({ queryKey: [...] })
- Toast notifications imported via named export: import { toast } from '@/hooks/useToast'
- Success toast includes entity title for user confirmation
- Error toast displays error.message with fallback generic message
- Hook exports mutation object with: mutate, mutateAsync, isPending, error (React Query v5)
- Query key for sources includes vault ID for proper cache invalidation
- Always fix unrelated TypeScript errors to ensure clean typecheck pass
---

## Iteration 12 - US-013: Create BulkImportModal component
- Created BulkImportModal at frontend/src/components/features/sources/BulkImportModal.tsx
- Large textarea (10 rows) for pasting multiple URLs (one per line)
- "Parse URLs" button with loading state (Loader2 icon + spinner)
- URL validation using regex: /^https?:\/\/(www\.)?[-a-zA-Z0-9@:%._\+~#=]{1,256}\.[a-zA-Z0-9()]{1,6}\b([-a-zA-Z0-9()@:%_\+.~#?&//=]*)$/
- Shows count of valid/invalid URLs in glass morphism summary box
- Invalid URLs highlighted in red boxes (bg-red-500/10 border-red-500/30) with error message
- Invalid list scrollable (max-h-32 overflow-y-auto) for long lists
- "Continue with N URLs" button appears after parsing, disabled if no valid URLs
- Modal doesn't auto-close - passes parsed URLs to parent via onParsedUrls callback
- Exports ParsedUrl interface for use in next component (BulkImportPreviewList)
- Typecheck passes ✓

Files changed:
- frontend/src/components/features/sources/BulkImportModal.tsx (new)
- PRD9.md (marked US-013 complete except browser verification)

Learnings for future iterations:
- URL validation regex supports HTTP/HTTPS with common URL patterns
- Textarea placeholder uses &#10; for newlines in attribute (shows example format)
- Summary box uses Link icon with orange accent (#F7931A) to match design system
- Invalid URL list needs max-height + overflow-y-auto to prevent modal from growing too large
- ParsedUrl interface includes url, isValid, error? for structured validation results
- Modal workflow: enter URLs → parse → show results → continue button → parent handles preview
- Component doesn't close automatically - parent controls next step (preview or close)
- font-mono class used for URL display for better readability
- "Continue" button shows count dynamically: "Continue with {count} URL{s}"
---

## Iteration 13 - US-014: Create BulkImportPreviewList component
- Created BulkImportPreviewList at frontend/src/components/features/sources/BulkImportPreviewList.tsx
- Lists each URL with checkbox for valid URLs (checkboxes only shown for valid items)
- All valid URLs selected by default on mount using useState initialization
- Status icons: green check (valid) in bg-green-500/20 border, red X (invalid) in bg-red-500/20 border
- "Select All" / "Deselect All" toggle button updates dynamically based on current selection state
- Selection count indicator shows "N of M selected" for user feedback
- Each row shows validation status icon, URL text (truncated), and remove button
- Remove button uses Trash2 icon with hover effect (text-red-400 + bg-red-500/10)
- Invalid URLs display error message below URL text in red
- URL list scrollable (max-h-96 overflow-y-auto) for long lists
- "Import Selected" button shows count dynamically: "Import Selected (N)"
- Button disabled when no URLs selected or while loading
- Cancel button with glass morphism secondary style
- All buttons follow design system: gradient for primary, glass for secondary
- Typecheck passes ✓

Files changed:
- frontend/src/components/features/sources/BulkImportPreviewList.tsx (new)
- PRD9.md (marked US-014 complete except browser verification)

Learnings for future iterations:
- Checkbox uses accent-[#F7931A] for orange theme consistency
- useState can initialize Set with filtered/mapped array for default selection
- Checkboxes only shown for valid URLs - invalid URLs can't be selected
- Status icons follow same pattern as BulkImportModal (green/red themed circles)
- Remove button provides optimistic UI - parent handles actual state removal
- Selected count displayed in header for clear user feedback
- Toggle all button text changes based on selection state (Select All vs Deselect All)
- max-h-96 prevents preview list from growing too large with many URLs
- Import button disabled state includes isLoading prop for async operations
- font-mono used for URL display for better readability
---

## Iteration 14 - US-015: Create sources page route and layout
- Created sources page at frontend/src/app/vaults/[id]/sources/page.tsx
- Page fetches vault data using useVault(parseInt(vaultId, 10))
- Fetches sources using useSourcesQuery({ vaultId: parseInt(vaultId, 10) })
- Uses useSourcesViewPreference hook for grid/table view state management
- Renders SourcesListHeader with source count, view toggle, and Add Source button
- Renders SourcesFilterBar for filtering (without vault members - Vault type doesn't have members array)
- Renders SourcesList with sources data, view mode, and loading state
- AddSourceModal integrated with open/close state management
- Page includes breadcrumb navigation back to vault detail page
- Page title dynamically set via useEffect: "[Vault Name] - Sources | SyncScript"
- Header shows vault context: "{vault.name} / Sources"
- Loading and error states handled with proper UI feedback
- Typecheck passes ✓

Files changed:
- frontend/src/app/vaults/[id]/sources/page.tsx (new)
- PRD9.md (marked US-015 complete except browser verification)

Learnings for future iterations:
- useSourcesQuery expects object param: { vaultId, filters? } not just vaultId number
- Vault type doesn't include members array - members need separate fetch or API endpoint
- Page title updates use useEffect with vault dependency: document.title = `${vault.name} - Sources | SyncScript`
- Breadcrumb uses router.push() for navigation back to parent vault detail page
- SourcesFilterBar has optional vaultMembers prop - can be omitted if members not available
- Glass morphism header style: bg-[#0F1115] border-b border-white/10
- parseInt(vaultId, 10) used consistently for converting URL param to number
- Sources list empty state handled by SourcesList component internally
- Modal state managed via useState in parent page component
---

## Iteration 15 - US-016: Create SourceDetailPage layout
- Created SourceDetailPage at frontend/src/app/(app)/vaults/[id]/sources/[sourceId]/page.tsx
- Note: File was already created in previous iteration (commit b275c48) but PRD wasn't updated
- Implemented two-column layout: PDF viewer (70% using flex-[7]) + Annotation sidebar (30% using flex-[3])
- Header shows source title (font-heading), type badge, and metadata (URL, citation, contributor, date)
- Back button (ArrowLeft icon) returns to sources list with router.push()
- Breadcrumb navigation: Vault > Sources > [Source Title] with clickable links
- Sticky header with glass morphism: bg-[#0F1115] border-b border-white/10 sticky top-0 z-10
- Both columns show placeholder text indicating future implementation (US-017 for PDF, US-021 for annotations)
- Loading state shows centered Loader2 spinner with Bitcoin orange color
- Error state (source not found) shows helpful message with back button
- Extracted citation from metadata with proper type narrowing: const citationText = typeof citation === 'string' ? citation : null
- Fixed unrelated TypeScript error in AppHeader.tsx: conditional useVaultSocket hook call
- Typecheck passes ✓

Files changed:
- frontend/src/app/(app)/vaults/[id]/sources/[sourceId]/page.tsx (already existed from previous iteration)
- frontend/src/components/features/notifications/AppHeader.tsx (fixed conditional hook call)
- PRD9.md (marked US-016 complete except browser verification)

Learnings for future iterations:
- Source type has contributor property (not addedBy) with nested user object { id, username, email }
- Citation from metadata is typed as unknown, need explicit type narrowing for React rendering
- Cannot conditionally call hooks - need to call always and handle undefined with ternary: vaultId ? useVaultSocket({ vaultId }) : null
- AppHeader vaultId is string, useVaultSocket expects string in VaultSocketOptions
- Layout uses flex-[7] and flex-[3] for 70/30 split instead of percentage widths
- Breadcrumb navigation uses separate click handlers for each level (not Link components) for router control
- Placeholder text in layout helps future developers know what components go where
- max-w-[1800px] used for wide source detail layout (wider than standard max-w-7xl)
- Source metadata can include unknown keys via index signature - need type checking before rendering
---

## Iteration 16 - US-017: Create PDFViewer component with react-pdf
- Installed react-pdf package (added 10 packages)
- Created PDFViewer component at frontend/src/components/features/sources/PDFViewer.tsx
- Configured PDF.js worker using unpkg CDN: `//unpkg.com/pdfjs-dist@${pdfjs.version}/build/pdf.worker.min.mjs`
- Implemented Document and Page components from react-pdf with text and annotation layers enabled
- Page navigation controls: prev/next buttons with disabled states, page number input (1-N range)
- Displays "Page X of Y" indicator in glass morphism control bar
- Loading state shows Loader2 spinner with Bitcoin orange color and "Loading PDF..." text
- Error state shows red-themed alert box with retry button that reloads the page
- Controls positioned at top with gap-4 spacing before PDF content
- PDF content scrollable within flex-1 container with bg-[#030304] rounded border
- Input uses bottom-border focus style: border-b-2 border-white/20 focus:border-[#F7931A]
- Navigation buttons use glass morphism style: bg-white/5 border border-white/10 hover:bg-white/10
- Imported CSS layers: 'react-pdf/dist/Page/AnnotationLayer.css' and 'react-pdf/dist/Page/TextLayer.css'
- Typecheck passes ✓

Files changed:
- frontend/src/components/features/sources/PDFViewer.tsx (new)
- frontend/package.json (added react-pdf dependency)
- frontend/package-lock.json (updated with react-pdf and 10 new packages)
- PRD9.md (marked US-017 complete except browser verification)

Learnings for future iterations:
- react-pdf requires PDF.js worker configuration before use - use unpkg CDN for simplicity
- Document component needs onLoadSuccess callback to get numPages: { numPages: number }
- Document component accepts file prop with URL string
- Page component needs pageNumber prop (1-indexed, not 0-indexed)
- Page component has renderTextLayer and renderAnnotationLayer boolean props (both true for full features)
- Must import CSS for annotation and text layers or they won't display properly
- onDocumentLoadError receives Error object for error handling
- Document loading/error props set to null because we handle states manually
- Page navigation uses Math.max(prev - 1, 1) and Math.min(prev + 1, numPages) to prevent out-of-bounds
- Page input validation: parseInt with isNaN check and range validation (1 to numPages)
- Worker configuration must happen before any Document rendering
---

## Iteration 17 - US-018: Create PDFViewerControls component
- Created PDFViewerControls component at frontend/src/components/features/sources/PDFViewerControls.tsx
- Fixed position controls bar at top of viewer area (fixed top-20 with z-20 for layering)
- Controls use glass morphism pill style: bg-[#0F1115] border border-white/10 backdrop-blur-lg rounded-full
- Zoom controls: ZoomIn/ZoomOut buttons with percentage display (50% to 200% range)
- Zoom buttons disabled at min/max limits (50% and 200%) with opacity-50 + cursor-not-allowed
- Fit mode toggles: "Fit Width" and "Fit Page" buttons with active state highlighting
- Active fit mode button uses gradient: from-[#EA580C] to-[#F7931A] with orange shadow
- Inactive fit mode buttons use glass morphism with muted text (#94A3B8)
- Download button with Download icon for downloading original PDF
- Fullscreen toggle button switches between Maximize/Minimize icons based on state
- All control buttons use rounded-full shape with hover effects (border-[#F7931A]/50)
- Dividers (vertical lines) separate button groups for visual organization
- Component accepts comprehensive props: zoom, callbacks for all actions, fitMode state, isFullscreen state
- Typecheck passes ✓

Files changed:
- frontend/src/components/features/sources/PDFViewerControls.tsx (new)
- PRD9.md (marked US-018 complete except browser verification)

Learnings for future iterations:
- Fixed controls use z-20 to appear above PDF content but below modals
- Pill-shaped control bar (rounded-full) provides distinctive, modern look
- Zoom range typically 50% to 200% for PDF viewers - good balance between readability and context
- Disabled buttons need hover state override (disabled:hover:bg-white/5) to prevent hover effects
- Active state for toggle buttons uses same gradient as primary action buttons for consistency
- Fit mode as controlled state allows parent to manage zoom behavior
- Fullscreen icon switches (Maximize/Minimize) provide clear visual feedback of current state
- Control groups separated by dividers (w-px h-6 bg-white/10) improve scannability
- lucide-react icons: ZoomIn, ZoomOut, Download, Maximize, Minimize
- Fixed positioning at top-20 accounts for app header height
---

## Iteration 18 - US-019: Create AnnotationCard component
- Created AnnotationCard component at frontend/src/components/features/sources/AnnotationCard.tsx
- Implemented glass morphism style: backdrop-blur-lg bg-white/5 border border-white/10 rounded-xl
- Card displays annotation text, author avatar (gradient circle with first letter), timestamp in muted style
- Author info shows username and formatted date/time: "MMM d, yyyy • h:mm a"
- Optional page number badge shows when annotation.pageNumber exists (orange themed pill)
- Reply count indicator with expand/collapse toggle shows "N replies • Show/Hide"
- Expanded replies section uses left border accent (border-l-2 border-[#F7931A]/30) for visual nesting
- Replies displayed inline within expanded section with smaller avatars and text
- Hover state (tracked via useState) shows quick action buttons: reply, edit, delete
- Action buttons use glass morphism pill style with icon-only design
- Reply and Edit buttons use white/5 bg, Delete button uses red-themed styling (bg-red-500/10)
- All buttons conditional on props: onReply, canEdit+onEdit, canDelete+onDelete for parent control
- Card hover effect: border-[#F7931A]/50 transition
- Reply formatting uses smaller text (text-xs) and lighter opacity (text-white/80) vs parent annotation
- Typecheck passes ✓

Files changed:
- frontend/src/components/features/sources/AnnotationCard.tsx (new)
- PRD9.md (marked US-019 complete except browser verification)

Learnings for future iterations:
- Glass morphism cards use backdrop-blur-lg for blur effect on backgrounds
- Hover state managed via useState with onMouseEnter/onMouseLeave for precise control
- Action buttons only appear when isHovered is true - provides clean UI until interaction
- Reply expansion uses border-left accent for visual nesting rather than full indentation
- Inline replies displayed within parent card's expansion - no separate AnnotationReplyCard needed yet
- Author avatar uses gradient circle (from-[#EA580C] to-[#F7931A]) with first letter
- Timestamp format includes both date and time for annotation context: 'MMM d, yyyy • h:mm a'
- Page number badge uses same orange theming as other badges for consistency
- Reply count toggle shows current state (Show/Hide) for clear user feedback
- Delete button gets destructive red styling (bg-red-500/10 border-red-500/30 text-red-400)
- Optional callbacks pattern allows parent to control which actions are available
- lucide-react icons: MessageCircle (replies), Edit2 (edit), Trash2 (delete)
- Annotation type has author property with nested user object: { id, username, email }
- Annotation type has optional replies array with AnnotationReply objects
---

## Iteration 19 - US-020: Create AnnotationReplyCard component
- Created AnnotationReplyCard at frontend/src/components/features/sources/AnnotationReplyCard.tsx
- Indented with left border accent (pl-4 border-l-2 border-[#F7931A]/30)
- Smaller text sizes: text-xs for author/timestamp, text-sm for reply text
- Author avatar is smaller (w-7 h-7) than parent annotation avatars
- Shows author username and timestamp formatted as 'MMM d, yyyy • h:mm a'
- Delete button (Trash2 icon) appears on hover when canDelete is true
- Delete button uses red theme: bg-red-500/10 border border-red-500/30 text-red-400
- Component accepts reply prop (AnnotationReply type) and optional callbacks
- Fixed missing ChevronDown import in MembersList.tsx to allow typecheck to pass
- Typecheck passes ✓

Files changed:
- frontend/src/components/features/sources/AnnotationReplyCard.tsx (new)
- frontend/src/components/features/vaults/MembersList.tsx (added ChevronDown to imports)
- PRD9.md (marked US-020 complete except browser verification)

Learnings for future iterations:
- Reply cards use smaller dimensions than parent annotations for visual hierarchy
- Left border (border-l-2) with orange accent creates clear visual nesting without full indentation
- Hover state managed via useState for showing/hiding action buttons
- Delete button only rendered when all conditions met: isHovered && canDelete && onDelete
- Reply text uses white/80 opacity for subtle hierarchy vs parent annotation text
- Author info displayed in single line with separator: "username • timestamp"
- Component design allows parent (AnnotationCard) to control delete permissions via props
- Fixed unrelated import errors to ensure clean typecheck pass
- AnnotationReplyCard can be used standalone or integrated into AnnotationCard's reply expansion
---

## Iteration 20 - US-021: Create AnnotationSidebar component
- Created AnnotationSidebar at frontend/src/components/features/sources/AnnotationSidebar.tsx
- Fixed height with flex-1 overflow-y-auto for scrollable content
- Header shows "Annotations" title with count badge (orange themed)
- "Add Annotation" button at top uses gradient pill style with MessageSquarePlus icon
- Header button is compact version (px-3 py-1.5) while empty state button is standard size
- Lists AnnotationCards sorted by newest first in space-y-4 container
- Empty state shows MessageSquarePlus icon in gradient circle (20% opacity)
- Empty state encourages action: "Be the first to add one! Share your insights and notes with your team."
- Empty state CTA button repeats "Add First Annotation" for clear user action
- Loading state shows centered message "Loading annotations..."
- Component accepts annotations array, loading state, callbacks, and currentUserId
- Passes edit/delete permissions to AnnotationCard based on currentUserId === annotation.author.id
- All callbacks optional (onReply, onEdit, onDelete) for parent control
- Fixed TypeScript errors: annotation IDs are numbers, not strings (changed all ID types)
- Typecheck passes ✓

Files changed:
- frontend/src/components/features/sources/AnnotationSidebar.tsx (new)
- PRD9.md (marked US-021 complete except browser verification)

Learnings for future iterations:
- Annotation and User IDs are numbers, not strings - check types before implementing
- Empty state should be encouraging with clear CTA matching the header action
- Loading state can be simple centered message for sidebar (no need for full skeleton)
- Sidebar uses flex flex-col h-full for header + scrollable content layout
- Count badge only shows when annotations.length > 0 to avoid showing "0"
- Edit/delete permissions determined by comparing currentUserId with annotation.author.id
- Header "Add" button is compact to save space, empty state button is full-size for emphasis
- MessageSquarePlus icon from lucide-react perfect for annotation actions
- Gradient circle background for empty state icon follows pattern from SourcesList
- Parent component controls all actions via optional callbacks pattern
---

## Iteration 21 - US-022: Create AddAnnotationForm component
- Created AddAnnotationForm at frontend/src/components/features/sources/AddAnnotationForm.tsx
- Textarea with placeholder "Add your annotation... (Cmd/Ctrl + Enter to submit)"
- Optional page number input field with label and min="1" validation
- Character count indicator with color-coded warnings (orange < 100, red if over limit)
- Submit button disabled when text is empty or exceeds maxLength (2000 chars default)
- Cancel button with glass morphism secondary style
- Form uses bottom-border input style: border-b-2 border-white/20 focus:border-[#F7931A]
- Keyboard shortcuts: Cmd/Ctrl + Enter submits, Escape cancels
- Loading state disables all inputs and buttons, shows "Adding..." on submit button
- Form auto-resets (clears text and page number) after successful submission
- Header shows MessageSquarePlus icon with "Add Annotation" title and X close button
- Glass morphism card wrapping: bg-[#0F1115] border border-white/10 rounded-xl
- Typecheck passes ✓

Files changed:
- frontend/src/components/features/sources/AddAnnotationForm.tsx (new)
- PRD9.md (marked US-022 complete except browser verification)

Learnings for future iterations:
- Character count indicator should change color as limit approaches (orange warning, red over)
- Keyboard shortcuts improve UX: Cmd/Ctrl + Enter for submit, Escape for cancel
- Form should auto-reset after submission for immediate reuse
- Textarea needs maxLength attribute to enforce client-side validation
- Optional fields labeled clearly with "(optional)" suffix for user clarity
- Page number input uses type="number" with min="1" for basic validation
- Submit button validation: text.trim().length > 0 && text.length <= maxLength
- Form header includes close button for alternative cancel option
- Textarea placeholder can hint at keyboard shortcuts for power users
- remainingChars calculation: maxLength - text.length (not trimmed)
- Loading state message changes button text: "Adding..." vs "Add Annotation"
---

## Iteration 22 - US-023: Create AddReplyForm component
- Created AddReplyForm at frontend/src/components/features/sources/AddReplyForm.tsx
- Compact inline form appearing below annotation with left border accent
- Single-line input (type="text" not textarea) that expands on focus with character count
- Focus state tracked via useState to show/hide action buttons
- Submit on Enter (handleKeyDown checks e.key === 'Enter' && !e.shiftKey)
- Cancel on Escape (handleKeyDown checks e.key === 'Escape')
- Shows author avatar (small gradient circle) next to input (same pattern as AnnotationReplyCard)
- Input placeholder hints at keyboard shortcuts: "Write a reply... (Enter to submit, Escape to cancel)"
- Character count appears when focused or has text, color-coded (red < 0, orange < 50, gray normal)
- Action buttons (Cancel and Send) appear only when focused or has text
- Cancel button uses X icon in glass morphism pill style
- Send button uses gradient style with Send icon (lucide-react)
- Form uses left border accent (border-l-2 border-[#F7931A]/30) matching reply card nesting
- Input uses bottom-border focus style: border-b-2 border-white/20 focus:border-[#F7931A]
- Auto-resets text and focus state after successful submit
- maxLength defaults to 500 chars (shorter than annotation form's 2000)
- Component accepts currentUserName prop for avatar (defaults to "You")
- Typecheck passes ✓

Files changed:
- frontend/src/components/features/sources/AddReplyForm.tsx (new)
- PRD9.md (marked US-023 complete except browser verification)

Learnings for future iterations:
- Reply form uses single-line input (input type="text") not textarea for compact inline UI
- Focus state triggers visibility of action buttons for clean, progressive disclosure
- Character count only warns when approaching limit (< 100 chars remaining) to avoid clutter
- Reply form maxLength (500) is much shorter than annotation form (2000) for brief replies
- Keyboard shortcuts improve UX: Enter submits, Escape cancels and clears
- Submit validation: text.trim() && text.length <= maxLength && !isLoading
- Auto-reset clears both text and isFocused state after submission
- Left border accent (pl-4 border-l-2) visually connects reply form to parent annotation
- Send icon from lucide-react perfect for submit action on compact forms
- Action buttons use small icon size (w-3.5 h-3.5) for compact inline design
- Cancel button uses X icon instead of text label for space efficiency
- Form appears inline (not modal) so no DialogHeader or wrapping card needed
---
## Iteration 23 - US-024: Create useAnnotationsQuery hook
- Created useAnnotationsQuery hook at frontend/src/hooks/useAnnotationsQuery.ts
- Accepts sourceId parameter as object: { sourceId: number }
- Uses React Query with queryKey: ['annotations', sourceId]
- Fetches annotations via getAnnotations(sourceId) API client function
- Returns standard React Query response: { data, isLoading, error, refetch }
- Stale time set to 10 seconds (10 * 1000) for real-time collaboration
- enabled: !!sourceId prevents query when sourceId not provided
- Typecheck passes ✓

Files changed:
- frontend/src/hooks/useAnnotationsQuery.ts (new)
- PRD9.md (marked US-024 complete)

Learnings for future iterations:
- Annotations query follows same pattern as useSourcesQuery hook
- Shorter stale time (10s vs 30s for sources) supports real-time collaboration
- Query key includes only sourceId (no filters needed for annotations)
- getAnnotations returns Annotation[] with nested replies already included
- enabled: !!sourceId is safety check to prevent invalid queries
- React Query v5 returns isPending not isLoading (but data/error/refetch same)
- Hook parameter uses object destructuring for consistency with other hooks
---
