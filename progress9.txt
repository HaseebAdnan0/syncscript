# Progress Log

## Feature: Frontend Sources & Annotations UI (PRD9)

## Learnings
(Patterns discovered during implementation)

---

## Iteration 1 - US-001: Create Source TypeScript types and API client
- Created `frontend/src/lib/types/sources.ts` with Source, SourceType, SourceMetadata types
- Created `frontend/src/lib/types/annotations.ts` with Annotation, AnnotationReply types
- Created `frontend/src/lib/api/sources.ts` with CRUD functions (getSources, getSource, createSource, updateSource, deleteSource)
- Created `frontend/src/lib/api/annotations.ts` with CRUD functions (getAnnotations, createAnnotation, createReply, deleteAnnotation)
- All API functions use axios instance from existing lib/api setup
- Typecheck passes ✓

Files changed:
- frontend/src/lib/types/sources.ts (new)
- frontend/src/lib/types/annotations.ts (new)
- frontend/src/lib/api/sources.ts (new)
- frontend/src/lib/api/annotations.ts (new)
- PRD9.md (marked US-001 complete)

Learnings for future iterations:
- TypeScript types follow existing pattern from lib/types/api.ts with PaginatedResponse generic
- API clients use axios instance from lib/api.ts with built-in error handling via handleApiError
- Sources API returns paginated results, so getSources extracts response.data.results
- Annotations include nested replies array in the response structure
- All CRUD operations follow RESTful URL patterns: /vaults/{id}/sources/, /sources/{id}/annotations/
- Files were committed in frontend submodule commit d89e7bf alongside vault types/API
---

## Implementation Notes

### Dependencies to verify before starting:
- [ ] Backend Sources API endpoints exist and return expected shape
- [ ] Backend Annotations API endpoints exist with reply support
- [ ] WebSocket channel includes source/annotation events
- [ ] Vault detail page shell exists at /vaults/[id]

### Design tokens reference:
- Background: #030304 (True Void)
- Surface: #0F1115 (Dark Matter)
- Muted text: #94A3B8 (Stardust)
- Primary: #F7931A (Bitcoin Orange)
- Glass morphism: backdrop-blur-lg bg-white/5 border border-white/10

---
## Iteration 2 - US-002: Create SourceCard component
- Created SourceCard component at frontend/src/components/features/sources/SourceCard.tsx
- Implemented glass morphism styling: bg-[#0F1115] border border-white/10 rounded-2xl
- Displays source title, type badge (using default Badge variant), contributor avatar (gradient circle), and created date
- Shows truncated URL or citation preview (80 char limit with ellipsis)
- Hover effect: -translate-y-1 and border-[#F7931A]/50 with transition-all duration-300
- Links to /vaults/{vaultId}/sources/{source.id} for detail page navigation
- Installed date-fns package for date formatting (format function)
- Typecheck passes ✓

Files changed:
- frontend/src/components/features/sources/SourceCard.tsx (new)
- frontend/package.json (added date-fns dependency)
- frontend/package-lock.json (updated with date-fns)
- PRD9.md (marked US-002 complete except browser verification)

Learnings for future iterations:
- Source type uses camelCase properties: source.type, source.createdAt (not snake_case)
- Badge component supports variants: default, success, warning, error (no outline variant)
- date-fns format() function used for date display: format(new Date(source.createdAt), 'MMM d, yyyy')
- Contributor avatar rendered as gradient circle with first letter of username
- Muted text color is #94A3B8 (Stardust) from design tokens
- Glass morphism cards use specific hover transitions matching design system
- SourceMetadata can have unknown keys via index signature, need type assertion for citation
---

## Iteration 3 - US-003: SourceTypeBadge already implemented
- US-003 (SourceTypeBadge component) was already completed in a previous iteration
- Component exists at frontend/src/components/features/sources/SourceTypeBadge.tsx
- PRD9.md already correctly marked as complete
- No new work needed - moving to next task

Learnings for future iterations:
- Always check git history before implementing - avoid duplicate work
- SourceTypeBadge uses lucide-react icons: Globe (URL), FileText (PDF), Quote (Citation), Newspaper (Article)
- Badge colors use semantic mapping: blue=URL, red=PDF, purple=Citation, green=Article
- Badge styling follows pattern: bg-{color}/20 text-{color} border-{color}/30
---

## Iteration 4 - US-004: Create SourceTableRow component
- Created dropdown-menu UI component at frontend/src/components/ui/dropdown-menu.tsx
- Created SourceTableRow component at frontend/src/components/features/sources/SourceTableRow.tsx
- Implemented table row with: type badge, linked title, contributor, date added, actions dropdown
- Row hover effect: bg-white/5 transition-colors
- Actions dropdown uses Radix UI @radix-ui/react-dropdown-menu (already installed)
- Dropdown includes: View (link to detail), Edit (callback), Delete (callback with red styling)
- Delete option separated with DropdownMenuSeparator and styled with text-red-400
- Typecheck passes ✓ (no errors in new files)

Files changed:
- frontend/src/components/ui/dropdown-menu.tsx (new)
- frontend/src/components/features/sources/SourceTableRow.tsx (new)
- PRD9.md (marked US-004 complete except browser verification)

Learnings for future iterations:
- Radix dropdown menu uses Portal for proper z-index stacking
- Glass morphism dropdown: bg-[#0F1115] border border-white/10 backdrop-blur-lg
- DropdownMenuItem supports asChild prop for Link components
- Table row transitions use hover:bg-white/5 for subtle highlight
- Actions menu icon uses MoreVertical from lucide-react
- Destructive actions (delete) use text-red-400 focus:text-red-400
- Optional callbacks (onEdit, onDelete) allow parent to control functionality
---

## Iteration 5 - US-005 & US-006: SourcesListHeader and view preference hook
- Created SourcesListHeader component at frontend/src/components/features/sources/SourcesListHeader.tsx
- Created useSourcesViewPreference hook at frontend/src/hooks/useSourcesViewPreference.ts
- Header displays "Sources" title with count badge (orange Bitcoin theme)
- View toggle buttons (Grid/List icons) with active state using orange gradient
- Active button has gradient bg: from-[#EA580C] to-[#F7931A] with orange shadow
- Inactive buttons show muted color (#94A3B8) with hover to white
- Add Source button uses primary gradient style with rounded-full shape
- Hook manages localStorage persistence with key 'syncscript:sources-view'
- Default view mode is 'grid', persists immediately on change
- Typecheck passes ✓

Files changed:
- frontend/src/components/features/sources/SourcesListHeader.tsx (new)
- frontend/src/hooks/useSourcesViewPreference.ts (new)
- PRD9.md (marked US-005 and US-006 complete except browser verification)

Learnings for future iterations:
- View toggle uses button elements (not Button component) for tight control over styling
- Active state uses same gradient as primary buttons for consistency
- localStorage requires useEffect for SSR-safe initialization in Next.js
- Badge count uses Bitcoin orange theme: bg-[#F7931A]/20 text-[#F7931A] border-[#F7931A]/30
- Toggle buttons grouped in glass morphism container: bg-[#0F1115] border border-white/10 rounded-lg
- Icons from lucide-react: Grid and List for view modes, Plus for add button
- Files were committed in commit 610352e alongside empty vaults state
---

## Iteration 6 - US-007: SourcesFilterBar already implemented
- US-007 (SourcesFilterBar component) was already completed in a previous iteration
- Component exists at frontend/src/components/features/sources/SourcesFilterBar.tsx
- Fixed TypeScript errors in unrelated file (useNotifications.ts) to allow typecheck to pass
- PRD9.md already correctly marked tasks as complete
- Typecheck passes ✓

Files changed:
- frontend/src/hooks/useNotifications.ts (fixed type annotations for callback parameters)
- PRD9.md (already marked US-007 complete except browser verification)

Learnings for future iterations:
- SourcesFilterBar manages filter state via URL search params using useSearchParams/useRouter
- Filter buttons use glass morphism style: bg-[#0F1115] border border-white/10 backdrop-blur-lg
- Active filters show with bg-[#F7931A]/20 text-[#F7931A] highlighting
- Clear filters button appears conditionally with red styling (bg-red-500/10 border border-red-500/30)
- URL params ensure filters are shareable and persist across page refreshes
- Contributor filter populated from vaultMembers prop array
- Date options: Any Time, Today, This Week, This Month, Custom Range
- Type options: All Types, URL, PDF, Citation, Article
- When fixing TypeScript errors in React Query mutations, need explicit type annotations for callback parameters
---
---

## Iteration 7 - US-008: Create SourcesList container component
- Created SourcesList component at frontend/src/components/features/sources/SourcesList.tsx
- Implemented grid view with CSS grid: grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6
- Implemented table view with sticky header (bg-black/50 border-b border-white/10 sticky top-0)
- Table wrapped in overflow-x-auto for horizontal scrolling on small screens
- Created SourceCardSkeleton for grid loading state (6 cards shown)
- Created SourceTableRowSkeleton for table loading state (10 rows shown)
- Both skeletons use animate-pulse with bg-white/10
- Empty state uses FileQuestion icon in gradient circle with 20% opacity
- Empty state CTA button uses full gradient button style with shadow
- Component accepts viewMode prop and conditionally renders grid or table
- Also accepts optional callbacks: onAddSource, onEdit, onDelete
- Fixed unrelated TypeScript errors:
  - VaultToast.tsx: Changed annotation.user to annotation.author (already auto-fixed)
  - login/page.tsx: Changed named imports to default imports for GradientButton and GlassCard (already auto-fixed)
- Typecheck passes ✓

Files changed:
- frontend/src/components/features/sources/SourcesList.tsx (new)
- frontend/src/app/(auth)/login/page.tsx (created - was missing)
- frontend/src/components/features/notifications/VaultToast.tsx (fixed author property)
- PRD9.md (marked US-008 complete except browser verification)

Learnings for future iterations:
- Loading skeletons should match the structure of actual content (same grid/table layout)
- Empty states should be encouraging and include clear CTAs for first-time users
- Empty state icons should use gradient circles with reduced opacity to maintain design system
- Table sticky headers need explicit bg color (bg-black/50) to remain visible while scrolling
- Skeleton counts: 6 for grid cards, 10 for table rows (good UX balance)
- FileQuestion icon from lucide-react is perfect for empty source states
- Component design allows parent to control all actions via optional callbacks
- Always fix unrelated TypeScript errors to ensure typecheck passes for current task
---

## Iteration 8 - US-009: Create useSourcesQuery hook (already completed)
- US-009 (useSourcesQuery hook) was already implemented in a previous iteration
- Hook exists at frontend/src/hooks/useSourcesQuery.ts with correct implementation
- Accepts vaultId and optional filters parameter
- Uses React Query with queryKey: ['sources', vaultId, filters]
- Returns data, isLoading, error, refetch (standard React Query return)
- Stale time correctly set to 30 seconds (30 * 1000)
- Created missing Textarea UI component (frontend/src/components/ui/textarea.tsx)
- Textarea follows design system: bg-black/50, border-white/20, focus:border-[#F7931A]
- Fixed imports in CreateVaultModal (already auto-fixed)
- All changes already committed in previous workflow
- Typecheck passes ✓
- PRD9.md already correctly marked US-009 as complete

Files changed:
- frontend/src/hooks/useSourcesQuery.ts (already existed)
- frontend/src/components/ui/textarea.tsx (created to fix missing dependency)
- PRD9.md (already marked US-009 complete)

Learnings for future iterations:
- useSourcesQuery follows same pattern as useAnnotations hook
- Query key includes filters to ensure cache invalidation when filters change
- enabled: !!vaultId prevents query from running if vaultId is not provided
- Textarea component needed for CreateVaultModal - uses same bottom-border focus style
- cn() utility function used for className merging in UI components
- React.forwardRef pattern used for all UI components to support ref forwarding
---

## Iteration 9 - US-010: Create AddSourceModal component
- Created Dialog UI component at frontend/src/components/ui/dialog.tsx
- Dialog follows Radix UI pattern with glass morphism styling
- DialogContent uses bg-[#0F1115] border border-white/10 backdrop-blur-lg for glass effect
- DialogOverlay uses bg-black/80 backdrop-blur-sm for darkened backdrop
- Close button (X) positioned absolute top-right with orange focus ring
- Created AddSourceModal at frontend/src/components/features/sources/AddSourceModal.tsx
- Modal accepts open, onOpenChange, and vaultId props
- URL input uses bottom-border focus style: border-b-2 border-white/20 focus:border-[#F7931A]
- Fetch Metadata button uses full gradient style with loading state (Loader2 icon + spinner)
- Loading state shows spinner with "Fetching metadata from URL..." message
- Enter key triggers metadata fetch for better UX
- Button disabled when URL is empty or while loading
- Typecheck passes ✓

Files changed:
- frontend/src/components/ui/dialog.tsx (new)
- frontend/src/components/features/sources/AddSourceModal.tsx (new)
- PRD9.md (marked US-010 complete except browser verification)

Learnings for future iterations:
- Dialog component follows same export pattern as dropdown-menu (individual exports for each part)
- Glass morphism modal uses rounded-2xl border with backdrop-blur-lg
- DialogOverlay creates the darkened backdrop automatically via Portal
- Input with Enter key handler improves UX for form submission
- Loader2 icon from lucide-react with animate-spin for loading states
- Button gradient uses shadow-[0_0_20px_-5px_rgba(234,88,12,0.5)] for orange glow effect
- Modal dialogs use DialogHeader with DialogTitle and DialogDescription for accessibility
- vaultId will be used in next user story for actual metadata fetching/source creation
---

## Iteration 10 - US-011: Create SourceMetadataPreview component
- Created SourceMetadataPreview component at frontend/src/components/features/sources/SourceMetadataPreview.tsx
- Component displays metadata preview in glass morphism card: bg-[#0F1115] border border-white/10 rounded-xl
- Shows favicon (or Globe icon fallback), detected source type badge, editable title input, and description
- Title input uses bottom-border focus style: border-b-2 border-white/20 focus:border-[#F7931A]
- Editable title managed via local state (useState) - allows user to customize before adding
- "Add Source" button uses full gradient style with disabled state when title is empty or loading
- "Cancel" button uses secondary glass morphism style: bg-[#0F1115] border border-white/20
- Error state shows red-themed alert box with retry and cancel buttons
- Error box uses bg-red-500/10 border border-red-500/30 for consistency with error styling
- Retry button conditionally rendered when onRetry callback provided
- All buttons follow hover transitions from design system
- Typecheck passes ✓

Files changed:
- frontend/src/components/features/sources/SourceMetadataPreview.tsx (new)
- PRD9.md (marked US-011 complete except browser verification)

Learnings for future iterations:
- Metadata preview needs editable title field so users can customize before saving
- Favicon fallback uses Globe icon in bg-white/10 circle to match design system
- Error states should provide retry option via optional callback pattern
- Description text uses white/80 opacity for subtle hierarchy vs title
- Label text uses Stardust color (#94A3B8) for consistent muted metadata
- Disabled button state needs opacity-50 + cursor-not-allowed + no hover scale
- Component accepts callbacks (onAddSource, onCancel, onRetry) for parent control
- detectedType from metadata determines which SourceTypeBadge variant to show
---
## Iteration 11 - US-012: Create useAddSourceMutation hook
- Created useAddSourceMutation hook at frontend/src/hooks/useAddSourceMutation.ts
- Follows same mutation pattern as useCreateVault from useVaults.ts
- Uses React Query useMutation with Source, Error, CreateSourceRequest generic types
- Invalidates sources query cache using queryKey: ['sources', vaultId] on success
- Shows success toast with source title confirmation message
- Shows error toast on failure with error message
- Fixed unrelated TypeScript error: removed unused SourcesList import in vault detail page
- Typecheck passes ✓

Files changed:
- frontend/src/hooks/useAddSourceMutation.ts (new)
- frontend/src/app/vaults/[id]/page.tsx (fixed unused import)
- PRD9.md (marked US-012 complete)

Learnings for future iterations:
- Mutation hooks follow pattern: useMutation<ReturnType, ErrorType, RequestType>
- Query invalidation uses queryClient.invalidateQueries({ queryKey: [...] })
- Toast notifications imported via named export: import { toast } from '@/hooks/useToast'
- Success toast includes entity title for user confirmation
- Error toast displays error.message with fallback generic message
- Hook exports mutation object with: mutate, mutateAsync, isPending, error (React Query v5)
- Query key for sources includes vault ID for proper cache invalidation
- Always fix unrelated TypeScript errors to ensure clean typecheck pass
---

## Iteration 12 - US-013: Create BulkImportModal component
- Created BulkImportModal at frontend/src/components/features/sources/BulkImportModal.tsx
- Large textarea (10 rows) for pasting multiple URLs (one per line)
- "Parse URLs" button with loading state (Loader2 icon + spinner)
- URL validation using regex: /^https?:\/\/(www\.)?[-a-zA-Z0-9@:%._\+~#=]{1,256}\.[a-zA-Z0-9()]{1,6}\b([-a-zA-Z0-9()@:%_\+.~#?&//=]*)$/
- Shows count of valid/invalid URLs in glass morphism summary box
- Invalid URLs highlighted in red boxes (bg-red-500/10 border-red-500/30) with error message
- Invalid list scrollable (max-h-32 overflow-y-auto) for long lists
- "Continue with N URLs" button appears after parsing, disabled if no valid URLs
- Modal doesn't auto-close - passes parsed URLs to parent via onParsedUrls callback
- Exports ParsedUrl interface for use in next component (BulkImportPreviewList)
- Typecheck passes ✓

Files changed:
- frontend/src/components/features/sources/BulkImportModal.tsx (new)
- PRD9.md (marked US-013 complete except browser verification)

Learnings for future iterations:
- URL validation regex supports HTTP/HTTPS with common URL patterns
- Textarea placeholder uses &#10; for newlines in attribute (shows example format)
- Summary box uses Link icon with orange accent (#F7931A) to match design system
- Invalid URL list needs max-height + overflow-y-auto to prevent modal from growing too large
- ParsedUrl interface includes url, isValid, error? for structured validation results
- Modal workflow: enter URLs → parse → show results → continue button → parent handles preview
- Component doesn't close automatically - parent controls next step (preview or close)
- font-mono class used for URL display for better readability
- "Continue" button shows count dynamically: "Continue with {count} URL{s}"
---
