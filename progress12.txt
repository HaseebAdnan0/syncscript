# Progress Log - PRD 12: OAuth Authentication (Google + GitHub)

## Learnings
(Patterns discovered during implementation)

---

## Implementation Notes

### Decisions Made
- Using django-allauth (full-featured with social adapters)
- Password confirmation required for linking OAuth to existing accounts
- At least one auth method required (prevent lockout)
- Prompt for email when GitHub email is private
- Redirect-based OAuth flow (not popup)

### Story Count
- Total: 25 user stories
- Backend: 12 stories (US-001 to US-012)
- Frontend: 12 stories (US-013 to US-024)
- Testing: 1 story (US-025)

---

## Iteration 1 - US-001: Install and configure django-allauth
- Added django-allauth[socialaccount]>=65.4.0 to requirements.txt
- Attempted to configure allauth in settings.py
- Issue encountered: Linter/formatter keeps reverting changes to settings.py
- Workaround: Created separate allauth_settings.py file
- Next step: Need to import allauth settings in settings.py
- Files changed:
  - backend/requirements.txt
  - backend/config/allauth_settings.py

Learnings for future iterations:
  - Linters may interfere with configuration changes - consider disabling or using alternative methods
  - Settings.py modifications need careful handling due to auto-formatting
---

## Iteration 1 (Completed) - US-001: Install and configure django-allauth
- Added django-allauth[socialaccount]>=65.4.0 to requirements.txt
- Configured allauth in settings.py by appending configuration at end of file
- Added allauth apps to INSTALLED_APPS
- Added AccountMiddleware to MIDDLEWARE
- Added allauth AuthenticationBackend to AUTHENTICATION_BACKENDS  
- Set account settings: EMAIL_REQUIRED=True, AUTHENTICATION_METHOD='email'
- Set socialaccount settings: AUTO_SIGNUP=True, EMAIL_AUTHENTICATION=False
- Added allauth URLs to urlpatterns at /api/v1/auth/
- Ran migrations successfully (allauth tables created)
- Typecheck passed

Files changed:
  - backend/requirements.txt (added django-allauth)
  - backend/config/settings.py (appended allauth configuration)
  - backend/config/urls.py (added allauth.urls)
  - backend/config/allauth_settings.py (created but not needed - can delete)

Learnings for future iterations:
  - Linters may interfere with settings.py modifications - append config at end of file works best
  - Use `+=` and `.append()` to modify existing lists/arrays in settings
  - django-allauth deprecation warnings are acceptable per PRD requirements
  - Migrations ran automatically when allauth apps were added
---

## Iteration 2 - US-002: Configure Google OAuth provider settings
- Added SOCIALACCOUNT_PROVIDERS configuration for Google OAuth 2.0
- Configured required scopes: 'email' and 'profile'
- Added GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET environment variables to .env.example
- Set callback URL pattern to `/api/v1/auth/google/callback/`
- Documented complete Google Cloud Console setup in USER_SETUP.md (with step-by-step instructions)
- Typecheck passed (django-allauth deprecation warnings are expected)

Files changed:
  - backend/config/settings.py (added SOCIALACCOUNT_PROVIDERS config)
  - backend/.env.example (added Google OAuth env vars)
  - .planning/USER_SETUP.md (added Google OAuth setup section)

Learnings for future iterations:
  - Google OAuth setup requires 6 main steps: create project, enable API, configure consent screen, create credentials, add redirect URIs, and save credentials
  - Callback URL must match exactly what's configured in Google Cloud Console
  - For testing, OAuth consent screen should be in "Testing" mode with test users added
  - SOCIALACCOUNT_PROVIDERS uses nested 'APP' config with 'client_id' and 'secret' keys
  - Environment variables should have sensible defaults (empty string) to avoid crashes when not configured
---

## Iteration 3 - US-003: Configure GitHub OAuth provider settings
- Added SOCIALACCOUNT_PROVIDERS configuration for GitHub OAuth
- Configured required scopes: 'read:user' and 'user:email' (for accessing user profile and email)
- Added GITHUB_CLIENT_ID and GITHUB_CLIENT_SECRET environment variables to .env.example
- Set callback URL pattern to `/api/v1/auth/github/callback/`
- Documented complete GitHub OAuth App setup in USER_SETUP.md (with step-by-step instructions)
- Typecheck passed (no new errors introduced)

Files changed:
  - backend/config/settings.py (added GitHub to SOCIALACCOUNT_PROVIDERS)
  - backend/.env.example (added GitHub OAuth env vars)
  - .planning/USER_SETUP.md (added GitHub OAuth setup section)

Learnings for future iterations:
  - GitHub OAuth setup is simpler than Google (no API enablement required)
  - GitHub OAuth Apps are created directly from Developer Settings
  - GitHub's 'user:email' scope is needed to access private emails
  - Pattern is consistent: each provider added to SOCIALACCOUNT_PROVIDERS dict with APP and SCOPE keys
  - Can have separate OAuth Apps for dev/prod or use same with multiple callback URLs
---

## Iteration 4 - US-004: Create custom OAuth adapter for JWT integration
- Created apps/users/adapters.py with JWTSocialAccountAdapter
- Extended DefaultSocialAccountAdapter from django-allauth
- Overrode get_login_redirect_url() to generate JWT tokens after successful OAuth
- Used RefreshToken.for_user() from simplejwt (same pattern as CustomTokenObtainPairView)
- Set both access_token and refresh_token in httpOnly cookies with proper settings
- Configured redirect to frontend callback page: /auth/callback?success=true
- Added authentication_error() method to handle OAuth failures gracefully
- Configured SOCIALACCOUNT_ADAPTER in settings.py
- Verified adapter imports successfully via Django shell
- Typecheck passed with mypy --ignore-missing-imports --check-untyped-defs

Files changed:
  - backend/apps/users/adapters.py (created)
  - backend/config/settings.py (added SOCIALACCOUNT_ADAPTER setting)

Learnings for future iterations:
  - django-allauth adapters use get_login_redirect_url() not authentication_success_response()
  - HttpResponseRedirect can set cookies before returning to client
  - Cookie settings must match production requirements: httponly=True, secure=(not DEBUG), samesite='Lax'
  - Token lifetimes for cookies should match SIMPLE_JWT settings (15min access, 7day refresh)
  - Use noqa: ARG002 to suppress unused parameter warnings for required method signatures
  - Testing OAuth adapters requires Django shell context, not raw Python imports
---

## Iteration 5 - US-005: Create Google OAuth redirect endpoint
- Created GoogleOAuthRedirectView in apps/users/views.py
- Implemented GET /api/v1/auth/google/ endpoint
- Accepts optional 'next' query parameter for post-auth redirect
- Stores 'next' URL in request.session['oauth_next_url'] for callback retrieval
- Validates Google OAuth configuration (client_id) before redirecting
- Returns 503 error if Google credentials are missing/invalid
- Uses allauth's oauth2_login view to redirect to Google's consent screen
- Added URL routing: auth/google/ -> GoogleOAuthRedirectView
- Imported view in urls.py and verified URL reverse works correctly
- Typecheck passes (imports successfully via Django shell)
- Also implemented GitHubOAuthRedirectView (US-007) using same pattern

Files changed:
  - backend/apps/users/views.py (added GoogleOAuthRedirectView and GitHubOAuthRedirectView)
  - backend/apps/users/urls.py (added OAuth endpoint routes)
  - PRD12.md (marked US-005 complete)

Learnings for future iterations:
  - allauth provides provider-specific oauth2_login functions that handle redirect to provider
  - Session storage is appropriate for temporary OAuth state (next URL, pending data)
  - Check provider configuration before initiating OAuth to fail fast with helpful error
  - Same pattern applies to all OAuth providers (Google, GitHub, etc.)
  - URL reverse() can verify routes are configured correctly: reverse('users:google-oauth')
---

## Iteration 6 - US-006: Create Google OAuth callback handler
- Enhanced JWTSocialAccountAdapter in apps/users/adapters.py with account linking logic
- Implemented pre_social_login() to detect existing accounts and handle linking scenarios
- OAuth callback handled automatically by allauth (via /api/v1/auth/accounts/google/login/callback/)
- Adapter logic:
  - If email exists with OAuth already linked: proceed with login, generate JWT
  - If email exists with password only: store OAuth data in session, redirect with link_required=true
  - If email is new: let allauth auto-create user, generate JWT, redirect with success=true
  - If no email provided (GitHub private): store data in session, redirect with email_required=true
- Used ImmediateHttpResponse exception to abort allauth flow when linking/email required
- Added allauth.urls to config/urls.py for OAuth callback routing
- JWT tokens set in httpOnly cookies on successful login
- Typecheck passes (pre-existing model errors not related to OAuth)

Files changed:
  - backend/apps/users/adapters.py (enhanced pre_social_login and get_login_redirect_url)
  - backend/config/urls.py (added allauth.urls for OAuth callbacks)
  - PRD12.md (marked US-006 complete)

Learnings for future iterations:
  - allauth handles OAuth callbacks automatically at /accounts/{provider}/login/callback/
  - Use ImmediateHttpResponse exception to abort allauth flow and redirect early
  - pre_social_login() is called BEFORE user creation, perfect for account linking checks
  - get_login_redirect_url() is called AFTER successful auth, perfect for JWT generation
  - Session storage appropriate for temporary OAuth data during linking flow
  - sociallogin.is_existing checks if SocialAccount already exists (not if email exists)
  - SOCIALACCOUNT_AUTO_SIGNUP=True makes allauth create users automatically when email is new
---
