# Progress Log for PRD6: File Storage & Cloud Integration

## Iteration 1 - US-001: Create PDFUpload database model
- Implemented PDFUpload model in backend/apps/sources/models.py
- Implemented VaultStorageUsage model for storage tracking
- Created basic Vault model in backend/apps/vaults/models.py (needed for FK)
- Files changed:
  - backend/apps/sources/models.py (added PDFUpload, VaultStorageUsage)
  - backend/apps/vaults/models.py (added Vault model)
  - backend/apps/sources/migrations/0001_initial.py (generated)
  - backend/apps/vaults/migrations/0001_initial.py (generated)
  - PRD6.md (marked US-001 complete)
- Learnings for future iterations:
  - Django models use descriptors - mypy errors for "Need type annotation" are normal
  - Pyright is the better type checker for Django (0 errors vs 27 mypy warnings)
  - When using OneToOneField as primary key, access via the field name (self.vault), not _id
  - Source model already existed from PRD3, reused it
  - Vault model was needed as dependency even though PRD2 isn't complete
  - Migrations can be created even without database connection (just warnings)
---

## Iteration 2 - US-002: Create VaultStorageUsage database model
- VaultStorageUsage was already implemented in Iteration 1 alongside PDFUpload
- Verified all acceptance criteria met:
  - Model exists in backend/apps/sources/models.py (lines 126-150)
  - All required fields present: vault (OneToOne), total_bytes, file_count, user_breakdown, last_updated
  - Migration created and included in 0001_initial.py
  - Typecheck passed in iteration 1
- Files changed:
  - PRD6.md (marked US-002 complete)
- Learnings for future iterations:
  - When implementing related models together, mark all corresponding US as complete
  - VaultStorageUsage uses OneToOneField as primary_key (no separate id field)
---

## Iteration 3 - US-003: Configure django-storages with Cloudflare R2
- Added boto3>=1.35 to backend/requirements.txt (django-storages[s3] was already present)
- Configured S3-compatible storage in backend/config/settings.py:
  - Conditional storage: uses S3Boto3Storage if AWS credentials provided, else local filesystem
  - AWS_S3_FILE_OVERWRITE=False (don't overwrite same-name files)
  - AWS_QUERYSTRING_AUTH=True (use presigned URLs)
  - AWS_QUERYSTRING_EXPIRE=900 (15-minute expiry)
  - AWS_S3_SIGNATURE_VERSION=s3v4 (required for some S3-compatible services)
- Updated .planning/USER_SETUP.md with comprehensive setup guide:
  - Cloudflare R2 setup (recommended)
  - AWS S3 setup (alternative)
  - Local filesystem fallback (no cloud credentials needed for dev)
- Files changed:
  - backend/requirements.txt (added boto3)
  - backend/config/settings.py (added S3 storage configuration)
  - .planning/USER_SETUP.md (updated cloud storage section)
  - PRD6.md (marked US-003 complete)
- Learnings for future iterations:
  - Django settings can check environment variables to conditionally enable features
  - USE_S3 flag pattern allows graceful fallback to local storage for development
  - Settings loaded successfully confirms no syntax errors
  - Pyright typecheck passed with 0 errors on modified files (pre-existing errors in other files not related)
---

## Iteration 4 - US-004: Create PDF validation function
- Created backend/apps/sources/validators.py with validate_pdf_file() function
- Implemented 5-layer security validation:
  1. File size check (max 50MB)
  2. Content-Type header validation
  3. Magic number verification (checks PDF header bytes)
  4. MIME type detection via python-magic (content-based, not extension-based)
  5. PDF structure validation via pypdf (ensures file can be parsed)
- Added dependencies to backend/requirements.txt:
  - python-magic>=0.4.27 (for MIME detection)
  - pypdf>=5.1.0 (for PDF parsing and validation)
- Files changed:
  - backend/apps/sources/validators.py (created)
  - backend/requirements.txt (added python-magic, pypdf)
  - PRD6.md (marked US-004 complete)
- Learnings for future iterations:
  - python-magic reads file content, not just extension - provides real MIME type
  - pypdf PdfReader can parse from BytesIO (in-memory file)
  - Always reset file.seek(0) after reading to prevent issues in subsequent operations
  - Multiple validation layers provide defense in depth against malicious files
  - Pyright typecheck passed with 0 errors
---

## Iteration 5 - US-005: Create presigned URL generation utilities
- Task was already completed in a previous session (commit 7773f05)
- Verified implementation in backend/apps/sources/storage.py:
  - generate_presigned_upload_url(vault_id, filename, content_type) → returns (url, file_key)
  - generate_presigned_download_url(file_key, original_filename) → returns presigned URL
  - Upload URLs expire in 1 hour (3600 seconds)
  - Download URLs expire in 15 minutes (900 seconds)
  - Uses SigV4 signature version via Config(signature_version='s3v4')
  - Download URLs include Content-Disposition header for attachment download
  - File paths use UUID to prevent collisions: vaults/{vault_id}/pdfs/{uuid}.pdf
- Verified implementation works:
  - Django system check passes (no issues)
  - Module imports successfully (no syntax errors)
  - PRD6.md already marked complete
- Learnings for future iterations:
  - Always check git history before re-implementing (git log -- <file>)
  - Presigned URLs are generated via boto3 generate_presigned_url() method
  - Upload URLs use 'put_object' operation, download URLs use 'get_object' operation
  - Content-Disposition header triggers browser download with original filename
---

## Iteration 6 - US-006: Create upload URL endpoint
- Task was already completed in a previous session (commit 440cc63)
- Verified implementation exists:
  - UploadURLRequestSerializer and UploadURLResponseSerializer in backend/apps/sources/serializers.py
  - PDFUploadViewSet.upload_url() action in backend/apps/sources/views.py
  - URL routing in backend/apps/sources/urls.py wired to config/urls.py
  - Permission checking via _check_vault_permission() helper
  - Django system check passes
- Files verified:
  - backend/apps/sources/serializers.py (serializers for request/response)
  - backend/apps/sources/views.py (ViewSet with upload_url action)
  - backend/apps/sources/urls.py (router configuration)
  - backend/config/urls.py (includes sources URLs)
  - PRD6.md (already marked complete)
- Learnings for future iterations:
  - Always check git history before re-implementing (git log -- <file>)
  - Previous sessions may have completed tasks without updating progress file
  - Django system check is sufficient to verify endpoint wiring
  - US-006 endpoint: POST /api/v1/sources/pdfs/upload-url/
---

## Iteration 7 - US-007: Create upload completion endpoint
- Task was already implemented in a previous session (commit 929f729)
- Verified implementation in backend/apps/sources/views.py:
  - complete() action method on PDFUploadViewSet
  - Endpoint: POST /api/v1/sources/pdfs/{upload_id}/complete/
  - Validates file_key in request body
  - Updates PDFUpload.processing_status to 'processing'
  - Includes TODO comment for Celery task trigger (placeholder as required)
  - Returns pdf_id, status, and message
  - Permission checking via _check_vault_permission()
- Updated PRD6.md to mark US-007 complete
- Django system check passes
- Files changed:
  - PRD6.md (marked US-007 complete)
- Learnings for future iterations:
  - Implementation was complete, just needed PRD update
  - Inline Serializer class pattern (CompletionRequestSerializer) is valid for simple one-off validators
  - TODO comments are acceptable for placeholder functionality (Celery task)
---

## Iteration 8 - US-008: Create download URL endpoint
- Implemented download_url() action method in PDFUploadViewSet
- Endpoint: GET /api/v1/sources/pdfs/{pdf_id}/download-url/
- Permission validation allows VIEWER, CONTRIBUTOR, or OWNER roles
- Uses generate_presigned_download_url() from storage.py
- Returns: download_url (15min expiry), expires_in, filename, file_size
- Content-Disposition header triggers browser download with original filename
- Files changed:
  - backend/apps/sources/views.py (added download_url action, imported generate_presigned_download_url)
  - PRD6.md (marked US-008 complete)
- Learnings for future iterations:
  - @action(detail=True) creates routes like /pdfs/{pk}/download-url/
  - _check_vault_permission() accepts custom required_roles list
  - pdf_upload.file.name gives the S3 object key stored in FileField
  - Presigned download URLs expire in 900 seconds (15 minutes)
  - Pyright typecheck passes with 0 errors
  - Django system check passes (security warnings are expected in dev mode)
---

## Iteration 9 - US-009: Create PDF serializers
- Verified existing serializer implementations in backend/apps/sources/serializers.py
- All required serializers already implemented in previous session:
  - UploadURLRequestSerializer (lines 6-20): vault_id, filename, file_size, content_type with validation
  - UploadURLResponseSerializer (lines 23-32): upload_id, upload_url, expires_in, callback_url
  - PDFUploadSerializer (lines 34-54): Full model serializer with related fields (vault_name, uploaded_by_username)
- Django system check passes (6 security warnings are expected in dev mode)
- Files changed:
  - PRD6.md (marked US-009 complete)
- Learnings for future iterations:
  - Serializers were already complete from prior work, just needed PRD update
  - PDFUploadSerializer includes useful computed fields like uploaded_by_username and vault_name via source attribute
  - read_only_fields protects sensitive fields like processing_status from client modification
  - Django system check validates all imports and wiring successfully
---

## Iteration 10 - US-010: Create post-upload Celery task for metadata extraction
- Task was already implemented in a previous session (commit 47b2c6e)
- Verified implementation in backend/apps/sources/tasks.py:
  - process_uploaded_pdf() Celery task with @shared_task decorator
  - Downloads PDF from S3 via boto3 into in-memory BytesIO
  - Parses PDF with pypdf.PdfReader
  - Extracts: pdf_title (from /Title or /Subject metadata), pdf_author (from /Author), page_count
  - Updates PDFUpload record with extracted fields
  - Sets processing_status to 'completed' on success, 'failed' on error
  - Configured with max_retries=3 and exponential backoff (2^retries * 60 seconds)
- Verified Celery configuration:
  - backend/config/celery.py: Celery app initialization with Redis broker
  - backend/config/__init__.py: Auto-imports celery_app when Django starts
  - backend/config/settings.py: CELERY_* settings configured with Redis backend
- Verified task trigger in backend/apps/sources/views.py:
  - PDFUploadViewSet.complete() imports and calls process_uploaded_pdf.delay()
  - Task triggered after upload completion with pdf_upload_id
- Django system check passes (6 security warnings expected in dev mode)
- Task import test passes: max_retries=3 confirmed
- Files changed:
  - None (already implemented, just verified)
- Learnings for future iterations:
  - Always check git log before re-implementing (git show HEAD:file)
  - Celery tasks use @shared_task decorator for auto-discovery
  - boto3 client can download S3 objects into in-memory BytesIO for processing
  - pypdf.PdfReader.metadata returns dict with /Title, /Author, /Subject keys
  - Exponential backoff: countdown=2^retries * base_delay provides good retry spacing
  - Django automatically loads config/__init__.py, which imports celery_app
---

## Iteration 11 - US-011: Add thumbnail generation to processing task
- Task was already implemented in a previous session (commit f463ad4)
- Verified implementation in backend/apps/sources/tasks.py:
  - Imported pdf2image.convert_from_bytes and PIL.Image
  - Added thumbnail generation after metadata extraction (lines 83-137)
  - Convert first page to image with pdf2image at 72 DPI, 300px width
  - Save as JPEG with 80% quality using Pillow
  - Upload to S3 at vaults/{vault_id}/thumbnails/{uuid}.jpg
  - Generate 7-day presigned URL (604800 seconds)
  - Store in PDFUpload.thumbnail_url field
  - Graceful error handling: thumbnail failures logged as warnings, don't fail task
- Verified dependencies in backend/requirements.txt:
  - pdf2image>=1.17.0 (already present)
  - Pillow>=11.1.0 (already present)
- Verified documentation in .planning/USER_SETUP.md:
  - Poppler utils installation instructions for Windows/Linux/macOS
  - Explains why poppler is needed (pdftoppm for pdf2image)
- PRD6.md already marked US-011 complete in commit f463ad4
- Files verified:
  - backend/apps/sources/tasks.py (thumbnail generation implemented)
  - backend/requirements.txt (dependencies present)
  - .planning/USER_SETUP.md (poppler docs already added)
  - PRD6.md (already marked complete)
- Learnings for future iterations:
  - Always check git log first: git log --oneline -10
  - Previous sessions may complete multiple tasks in parallel
  - pdf2image requires poppler-utils system dependency (not a Python package)
  - Thumbnail generation wrapped in try-except to prevent task failure on thumbnail errors
  - presigned URLs for thumbnails use longer expiry (7 days) than download URLs (15 min)
  - S3 upload via upload_fileobj() requires BytesIO.seek(0) after saving image
---

## Iteration 12 - US-012: Create multipart upload initiate endpoint
- Implemented multipart upload initiation for large PDF files (>20MB)
- Created serializers in backend/apps/sources/serializers.py:
  - MultipartUploadRequestSerializer: validates vault_id, filename, file_size, part_size, content_type
  - MultipartUploadResponseSerializer: returns upload_id, pdf_upload_id, file_key, part_urls, expires_in
  - Validation: part_size minimum 5MB, maximum 10,000 parts (AWS S3 limit)
- Added initiate_multipart_upload() utility in backend/apps/sources/storage.py:
  - Calls s3_client.create_multipart_upload() via boto3
  - Generates presigned URL for each part using upload_part operation
  - Calculates number of parts: ceiling division (file_size + part_size - 1) // part_size
  - Returns upload_id (S3 multipart ID), file_key, and list of {part_number, upload_url}
- Implemented multipart_upload_initiate() action in backend/apps/sources/views.py:
  - Endpoint: POST /api/v1/sources/pdfs/multipart-upload/initiate/
  - @action(detail=False) creates route /pdfs/multipart-upload/initiate/
  - Validates user has contributor/owner vault permission
  - Creates PDFUpload record with status='pending'
  - Returns all data needed for client to upload parts individually
- Files changed:
  - backend/apps/sources/serializers.py (added multipart serializers)
  - backend/apps/sources/storage.py (added initiate_multipart_upload)
  - backend/apps/sources/views.py (added multipart_upload_initiate action)
  - PRD6.md (marked US-012 complete)
- Learnings for future iterations:
  - AWS S3 multipart upload has maximum 10,000 parts limit
  - Minimum part size is 5MB (except last part)
  - boto3 create_multipart_upload returns UploadId needed for completion
  - Each part gets its own presigned URL via upload_part operation
  - Part numbers start at 1 (not 0) in S3 multipart API
  - Ceiling division formula: (a + b - 1) // b
  - Django system check passed (6 security warnings expected in dev mode)
  - Pyright errors on Meta class overrides are false positives (normal Django pattern)
---

## Iteration 13 - US-013: Create multipart upload complete endpoint
- Implemented multipart upload completion for large PDF files
- Created MultipartUploadCompleteRequestSerializer in backend/apps/sources/serializers.py:
  - Validates pdf_upload_id (UUID) and parts array
  - Each part must have part_number (int 1-10000) and etag (str)
  - Enforces required fields and type checking
- Added complete_multipart_upload() utility in backend/apps/sources/storage.py:
  - Calls s3_client.complete_multipart_upload() via boto3
  - Formats parts with capitalized keys (PartNumber, ETag) for S3 API
  - Sorts parts by part_number (S3 requirement)
  - Combines all uploaded parts into final S3 object
- Implemented multipart_upload_complete() action in backend/apps/sources/views.py:
  - Endpoint: POST /api/v1/sources/pdfs/multipart-upload/{upload_id}/complete/
  - @action(detail=False) with custom URL pattern using upload_id path parameter
  - Validates user has contributor/owner vault permission
  - Completes S3 multipart upload with error handling
  - Updates PDFUpload status to 'processing' on success, 'failed' on error
  - Triggers process_uploaded_pdf Celery task for metadata extraction
  - Returns pdf_id, status, and success message
- Files changed:
  - backend/apps/sources/serializers.py (added MultipartUploadCompleteRequestSerializer)
  - backend/apps/sources/storage.py (added complete_multipart_upload)
  - backend/apps/sources/views.py (added multipart_upload_complete action, imported new serializer and storage function)
  - PRD6.md (marked US-013 complete)
- Learnings for future iterations:
  - S3 complete_multipart_upload requires parts sorted by PartNumber
  - Parts must be formatted with capitalized keys: PartNumber, ETag (not snake_case)
  - Custom URL patterns in @action decorator: url_path=r'multipart-upload/(?P<upload_id>[^/.]+)/complete'
  - upload_id from S3 (not stored in DB) passed via URL, pdf_upload_id passed in body
  - Error handling critical for multipart completion: set status to 'failed' if S3 operation fails
  - Django system check passes (0 errors)
  - Pyright warnings about unaccessed parameters in destroy() are false positives (required by Django signature)
---

## Iteration 14 - US-014: Create storage usage calculation utility
- Created backend/apps/sources/utils.py with update_vault_storage_usage() function
- Implemented storage usage aggregation from non-deleted PDFUploads:
  - total_bytes: Sum of all file_size fields
  - file_count: Count of all PDF uploads
  - user_breakdown: JSON dict mapping user_id to bytes used
- Used Django ORM aggregate functions (Sum, Count) for efficient queries
- update_or_create() pattern ensures VaultStorageUsage record exists
- Invalidates Redis cache key vault_storage:{vault_id} after update
- Files changed:
  - backend/apps/sources/utils.py (created)
  - PRD6.md (marked US-014 complete)
- Learnings for future iterations:
  - Django aggregate() returns dict with keys matching field names
  - aggregate() returns None for empty querysets - use 'or 0' for defaults
  - values() + annotate() pattern calculates per-group aggregates
  - update_or_create(defaults={}) updates fields only if record exists
  - cache.delete() is idempotent (safe to call even if key doesn't exist)
  - Django shell imports work with 'python manage.py shell -c "code"'
  - Pyright import errors for Django are normal without stubs installed
---
## Iteration 15 - US-015: Add storage fields to Vault API
- Added three SerializerMethodFields to VaultSerializer in backend/apps/vaults/serializers.py:
  - storage_used_bytes: returns total_bytes from related VaultStorageUsage (0 if doesn't exist)
  - storage_file_count: returns file_count from related VaultStorageUsage (0 if doesn't exist)
  - storage_user_breakdown: returns user_breakdown JSON dict (empty dict if doesn't exist)
- VaultStorageUsage has OneToOneField with related_name='storage_usage'
- Access pattern: obj.storage_usage.total_bytes (raises exception if doesn't exist)
- Error handling: try-except blocks return defaults (0, 0, {}) when VaultStorageUsage doesn't exist
- Updated VaultSerializer.Meta.fields to include new storage fields
- Files changed:
  - backend/apps/vaults/serializers.py (added 3 SerializerMethodField methods)
  - PRD6.md (marked US-015 complete)
- Learnings for future iterations:
  - OneToOneField with related_name='storage_usage' accessed as vault.storage_usage
  - OneToOneField raises RelatedObjectDoesNotExist when related object missing
  - SerializerMethodField pattern: get_<field_name>(self, obj) returns computed value
  - Generic Exception catch handles both DoesNotExist and AttributeError gracefully
  - Django system check passes validates all serializer configuration
  - Testing with mock objects useful to verify error handling without database
---

## Iteration 16 - US-016: Create soft-delete endpoint
- Implemented destroy() method in PDFUploadViewSet in backend/apps/sources/views.py
- Endpoint: DELETE /api/v1/sources/pdfs/{pdf_id}/
- Permission validation: Allows vault owner OR uploader OR vault admin (owner role via membership)
- Three permission checks:
  1. Direct vault owner (vault.owner == user)
  2. Original uploader (uploaded_by == user)
  3. Admin via membership (VaultMembership with role=OWNER)
- Soft-delete via deleted_at timestamp using timezone.now()
- Calls update_vault_storage_usage() to recalculate storage after deletion
- Returns: message, pdf_id, permanent_deletion_date (deleted_at + 30 days)
- Files changed:
  - backend/apps/sources/views.py (added destroy method, imports for timezone, timedelta, update_vault_storage_usage)
  - PRD6.md (marked US-016 complete)
- Learnings for future iterations:
  - ViewSet destroy() method signature: destroy(self, request, *args, **kwargs)
  - Use timezone.now() not datetime.now() for Django timezone-aware timestamps
  - timedelta(days=30) for calculating future dates
  - get_object() uses queryset filtering (deleted_at__isnull=True) from ViewSet definition
  - Permission can be multi-layered: owner OR uploader OR admin role
  - Django system check passes (6 security warnings expected in dev mode)
  - Python syntax check passes (py_compile validation)
---
## Iteration 16 - US-016: Create soft-delete endpoint
- Implemented destroy() method in PDFUploadViewSet
- Permission validation: allows vault owner OR uploader to delete (either one is sufficient)
- Also allows vault OWNER role members (via VaultMembership) to delete
- Sets deleted_at timestamp using timezone.now() for soft-delete
- Calls update_vault_storage_usage() to recalculate storage after deletion
- Returns success message, pdf_id, and permanent_deletion_date (30 days from now)
- Files changed:
  - backend/apps/sources/views.py (added destroy method to PDFUploadViewSet)
  - PRD6.md (marked US-016 complete)
- Learnings for future iterations:
  - Soft-delete: set deleted_at timestamp, don't actually delete from DB
  - Permission check needs OR logic: vault owner OR uploader OR vault admin member
  - Storage usage must be recalculated after deletion (affects vault quota tracking)
  - timedelta(days=30) adds 30 days to current timestamp for retention period
  - .isoformat() converts datetime to ISO 8601 string format (YYYY-MM-DDTHH:MM:SS)
  - Django system check passes with 6 security warnings (expected in dev mode)
  - ViewSet.destroy() uses self.get_object() which automatically filters by queryset
---

## Iteration 17 - US-017: Create cleanup_deleted_pdfs Celery task
- Implemented cleanup_deleted_pdfs() as @shared_task in backend/apps/sources/tasks.py
- Queries PDFUploads where deleted_at < 30 days ago using Django ORM filter
- Deletes PDF file from S3 via boto3 s3_client.delete_object()
- Deletes thumbnail from S3 by extracting thumbnail key from PDF file path pattern
- Deletes database record via pdf.delete() after S3 cleanup
- Returns dict with deleted_count and errors list for monitoring
- Comprehensive logging for each permanent deletion and errors
- Files changed:
  - backend/apps/sources/tasks.py (added cleanup_deleted_pdfs task, imported timezone and timedelta)
  - PRD6.md (marked US-017 complete)
- Learnings for future iterations:
  - Use timezone.now() - timedelta(days=30) to calculate date 30 days ago
  - Query with deleted_at__lt=threshold to find old records
  - S3 delete operations are idempotent (won't error if file doesn't exist)
  - Thumbnail key extraction: parse file.name to get vault_id, construct thumbnails/{uuid}.jpg
  - Best-effort thumbnail deletion: log warnings but don't fail task
  - select_related('vault') optimizes queries when accessing related objects in loop
  - Python syntax check passed (ast.parse validation)
  - Django system check passed (6 security warnings expected in dev mode)
---

## Iteration 18 - US-018: Create cleanup_orphaned_multipart_uploads Celery task
- Task was already implemented in a previous session (found in commit 87cb5b0)
- Verified implementation in backend/apps/sources/tasks.py:
  - cleanup_orphaned_multipart_uploads() as @shared_task on line 437
  - Lists all in-progress multipart uploads via s3_client.list_multipart_uploads()
  - Calculates 24-hour threshold using timezone.now() - timedelta(hours=24)
  - Aborts uploads where Initiated < threshold via s3_client.abort_multipart_upload()
  - Returns dict with aborted_count and errors list for monitoring
  - Comprehensive logging for each aborted upload and errors
  - Only runs when USE_S3 flag is enabled (graceful skip for local development)
- PRD6.md already marked US-018 complete in HEAD
- Files verified:
  - backend/apps/sources/tasks.py (implementation exists, lines 437-540)
  - PRD6.md (already marked complete)
- Learnings for future iterations:
  - Always check git log first: git log --oneline -10
  - Always check git show HEAD:<file> to compare with current commit
  - Previous iterations may complete multiple tasks in a single commit
  - S3 list_multipart_uploads returns all in-progress uploads for bucket
  - boto3 returns timezone-aware datetime objects from S3 API (Initiated field)
  - Comparison with threshold is direct: initiated < threshold (no conversion needed)
  - abort_multipart_upload requires Bucket, Key, and UploadId parameters
  - Task is idempotent and safe to run repeatedly (S3 operations handle missing uploads)
  - Django system check passes (6 security warnings expected in dev mode)
---

## Iteration 19 - US-019: Configure Celery Beat schedule for cleanup tasks
- Added Celery Beat schedule configuration to backend/config/celery.py
- Imported crontab from celery.schedules for scheduling
- Configured two periodic tasks in app.conf.beat_schedule:
  - cleanup-deleted-pdfs-daily: runs cleanup_deleted_pdfs task daily at 2:00 AM
  - cleanup-orphaned-multipart-uploads-daily: runs cleanup_orphaned_multipart_uploads task daily at 3:00 AM
- Used crontab(hour=2, minute=0) and crontab(hour=3, minute=0) for precise scheduling
- Task paths use full dotted path: apps.sources.tasks.cleanup_deleted_pdfs
- Files changed:
  - backend/config/celery.py (added beat_schedule configuration)
  - PRD6.md (marked US-019 complete)
- Learnings for future iterations:
  - Celery Beat requires celery.schedules.crontab import for cron-style scheduling
  - app.conf.beat_schedule is a dict mapping schedule names to task configurations
  - Each schedule entry has 'task' (dotted path to task) and 'schedule' (crontab object)
  - Task path format: apps.appname.tasks.task_function_name (not @shared_task decorator name)
  - crontab(hour=X, minute=Y) creates daily schedule at specific time
  - Pyright typecheck passes with 0 errors (no type annotations needed for Celery config)
  - Django system check passes (6 security warnings expected in dev mode)
  - Celery Beat worker must be started separately: celery -A config beat -l info
---

## Iteration 20 - US-020: Add WebSocket notifications for PDF events
- Task was already implemented in previous sessions (commits ccda158 and 50eb6e3)
- Verified implementation in backend/apps/sources/tasks.py:
  - Added import: from core.websocket_utils import broadcast_to_vault
  - In process_uploaded_pdf task (after successful processing):
    - Sends pdf.uploaded event to vault_{vault_id} WebSocket group
    - Payload includes: pdf_id, filename, pdf_title, page_count, thumbnail_url
    - User context: pdf_upload.uploaded_by
  - Error handling: WebSocket failures logged as warnings but don't fail the task
- Verified implementation in backend/apps/sources/views.py:
  - Added import: from core.websocket_utils import broadcast_to_vault
  - In PDFUploadViewSet.destroy() method (after soft-delete):
    - Sends pdf.deleted event to vault_{vault_id} WebSocket group
    - Payload includes: pdf_id, filename
    - User context: request.user
  - Error handling: WebSocket failures logged as warnings but don't fail the request
- Uses existing broadcast_to_vault() utility from core/websocket_utils.py
- Channel layer infrastructure already configured via Django Channels + Redis
- PRD6.md already marked US-020 complete
- Files changed:
  - None (already implemented in previous sessions)
- Learnings for future iterations:
  - Always check git log -S "pattern" to find when code was added
  - WebSocket broadcasts should not fail the main operation (try-except wrapper)
  - broadcast_to_vault() handles: sequence numbering, message envelope, group send, Redis buffering
  - Event types follow pattern: resource.action (e.g., pdf.uploaded, pdf.deleted)
  - Payload should include minimal identifying info (id, filename) and relevant metadata
  - User context optional but helpful for client-side filtering and display
  - Django system check passes (validates all imports and configuration)
---

## Iteration 21 - US-021: Add audit logging for file operations
- Implemented audit logging for all four required PDF operations
- Added AuditLog.objects.create() calls in tasks.py and views.py:
  1. pdf.uploaded: Logged after successful PDF processing in process_uploaded_pdf task
  2. pdf.processing_failed: Logged when PDF processing fails (with retry count)
  3. pdf.downloaded: Logged in download_url view when user requests download URL
  4. pdf.deleted: Logged in destroy view when PDF is soft-deleted
- Each audit log includes:
  - vault: ForeignKey to Vault
  - actor: ForeignKey to User (uploaded_by or request.user)
  - action: Event type string (pdf.uploaded, pdf.deleted, etc.)
  - metadata: JSONField with pdf_id, filename, and event-specific details
- Error handling: All audit log calls wrapped in try-except to prevent operation failures
- Files changed:
  - backend/apps/sources/tasks.py (added audit logging for uploaded and processing_failed events)
  - backend/apps/sources/views.py (added audit logging for downloaded and deleted events)
  - PRD6.md (marked US-021 complete)
- Learnings for future iterations:
  - Audit logging should not fail the main operation (use try-except wrapper)
  - AuditLog model already existed in apps.vaults.models (reused existing infrastructure)
  - metadata JSONField stores event-specific details (flexible schema)
  - actor can be null (SET_NULL) but always provide it when user context available
  - Django system check passes (0 errors)
  - Linters may remove unused imports - need to re-add if they're used in the code
  - Audit logs for task failures should include retry count for debugging
---
