# Progress Log

## Learnings
(Patterns discovered during implementation)
- Use `python manage.py check` for Django system validation (equivalent to typecheck)
- Use `python manage.py shell -c "command"` to verify URL routing
- Check git history before implementing - code may already be committed

---

## Iteration 1 - Add onboarding fields to User model (US-001)
- Added 4 onboarding fields to User model: onboarding_completed, onboarding_step, onboarding_data, onboarding_path
- Generated migration: 0002_user_onboarding_completed_user_onboarding_data_and_more.py
- Applied migration successfully
- Files changed: backend/apps/users/models.py, migration file, PRD20.md
- Learnings for future iterations:
  - User model is in backend/apps/users/models.py
  - Use `python manage.py check` for Django system validation (equivalent to typecheck)
  - JSONField default=dict for flexible metadata storage
  - CharField choices defined as tuples inline in model definition
---

## Iteration 2 - Create onboarding progress API endpoint (US-002)
- Verified US-002 was already implemented in previous commit (7075aa6)
- Created OnboardingSerializer with step, completed, path, data fields
- Created OnboardingView with GET and PATCH methods at /api/v1/users/me/onboarding/
- Added URL routing for onboarding endpoints
- Created comprehensive test suite: OnboardingAPITests with 11 test methods
- Files changed: backend/apps/users/serializers.py, views.py, urls.py, tests.py, PRD20.md
- Learnings for future iterations:
  - Always check git history before implementing to avoid duplicate work
  - Serializer with partial=True allows optional field updates
  - Merge existing JSONField data instead of replacing: `{**existing_data, **new_data}`
  - Use `update_fields` parameter in save() for efficiency
  - URL verification: `python manage.py shell -c "from django.urls import reverse; print(reverse('users:onboarding'))"`
---

## Iteration 3 - Create demo vault fixture data (US-003)
- Created backend/apps/users/fixtures/demo_vault.json with demo vault template
- Included 10 real AI research papers from arXiv (freely available)
- Papers cover: Transformers, GPT-3, CLIP, PaLM, GPT-4, Llama 2, Constitutional AI, Chinchilla, Tree of Thoughts, BitNet
- Added 18 annotations demonstrating threading (6 with parent references), citations, and metadata
- Created load_demo_vault.py management command to validate fixture structure
- Fixture structured as JSON template (not Django loaddata format) for use by service layer
- Validation passed: 10 sources, 18 annotations (6 threaded)
- Files changed: backend/apps/users/fixtures/demo_vault.json, load_demo_vault.py, PRD20.md
- Learnings for future iterations:
  - Fixtures for programmatic use should be JSON templates, not Django loaddata format
  - Use source_index/parent_index for relationships instead of foreign keys
  - Management commands useful for validation without modifying database
  - arXiv papers are ideal demo content (freely available, real research)
  - Annotations should demonstrate various features: threading, cross-references, metadata
---

## Iteration 4 - Create demo vault service (US-004)
- Created backend/apps/users/services/onboarding.py with create_demo_vault(user) function
- Service loads demo_vault.json fixture and creates vault with sources and annotations
- Implemented idempotency check - returns existing vault if already created for user
- Transaction-based creation ensures atomicity (all-or-nothing)
- Resolved index-based relationships for annotations (source_index, parent_index)
- Created comprehensive test suite: DemoVaultServiceTests with 5 test methods in test_onboarding.py
- Files changed: backend/apps/users/services/__init__.py, onboarding.py, tests/test_onboarding.py, PRD20.md
- All tests pass (5/5)
- Learnings for future iterations:
  - Vault creation signals auto-create owner membership - don't duplicate
  - Transaction.atomic() ensures rollback on error during multi-step creation
  - Index-based relationships in fixtures resolve to actual objects during creation
  - Test idempotency by calling service twice and verifying same vault returned
  - Pre-existing import error in sources.services fixed during this iteration
---

## Iteration 5 - Create demo vault reset endpoint (US-005)
- Created DemoVaultResetView at POST /api/v1/users/me/demo-vault/reset/
- Endpoint deletes existing demo vault and creates fresh one from template
- Added URL routing: users/me/demo-vault/reset/
- Created comprehensive test suite: DemoVaultResetAPITests with 5 test methods
- Fixed FK constraint violation by disconnecting membership deletion signal during vault deletion
- Files changed: backend/apps/users/views.py, urls.py, tests/test_onboarding.py, PRD20.md
- All tests pass (5/5)
- Learnings for future iterations:
  - Vault deletion triggers post_delete signals on VaultMembership that try to create audit logs
  - AuditLog has FK to Vault with CASCADE, but signals fire AFTER vault is deleted
  - Solution: Temporarily disconnect post_delete signal during vault deletion using try/finally
  - Pattern: `post_delete.disconnect(handler, sender=Model)` before delete, reconnect in finally
  - Test cascade deletion by capturing IDs before delete, verifying they don't exist after
  - VaultSerializer context={'request': request} needed for user_role field computation
---

## Iteration 6 - Prompt to recreate deleted demo vault (US-006)
- Created DemoVaultStatusView at GET /api/v1/users/me/demo-vault/status/
- Created DemoVaultCreateView at POST /api/v1/users/me/demo-vault/create/
- Status endpoint returns { exists: boolean, vault_id: string | null }
- Create endpoint creates vault only if it doesn't exist, returns error if already exists
- Added URL routing for both endpoints in users/urls.py
- Created comprehensive test suite: DemoVaultStatusAPITests (3 tests) and DemoVaultCreateAPITests (4 tests)
- Files changed: backend/apps/users/views.py, urls.py, tests/test_onboarding.py, PRD20.md
- All 7 tests pass, typecheck passes
- Learnings for future iterations:
  - Status endpoint pattern: return boolean existence flag plus optional ID
  - Create endpoint should validate non-existence before creating to prevent duplicates
  - Idempotent create_demo_vault service already handles duplicates, but explicit check gives better error message
  - Test both exists/not-exists scenarios for status endpoints
  - Test both success and already-exists scenarios for create endpoints
---

## Iteration 7 - Create OnboardingProvider context (US-007)
- Created OnboardingState and OnboardingUpdateData TypeScript types in lib/types/onboarding.ts
- Added 5 onboarding API client functions to lib/api.ts:
  - getOnboardingState(), updateOnboardingState(), getDemoVaultStatus(), createDemoVault(), resetDemoVault()
- Created OnboardingProvider context in providers/OnboardingProvider.tsx
- Provider fetches onboarding state on mount for authenticated users
- Provides: step, completed, path, data, isLoading, error, updateOnboarding(), completeOnboarding(), refetch()
- State changes synced to backend via PATCH /users/me/onboarding/
- Handles loading and error states with proper error messages
- Files changed: lib/types/onboarding.ts (new), lib/api.ts, providers/OnboardingProvider.tsx (new), PRD20.md
- Typecheck passes
- Learnings for future iterations:
  - React Context pattern: createContext + useContext hook for type safety
  - Fetch state on mount with useEffect + useCallback to prevent infinite loops
  - Reset state to defaults when user logs out (!isAuthenticated)
  - Provide both granular update method and convenience methods (completeOnboarding)
  - Include refetch method for manual state refresh scenarios
  - API imports require proper type imports from lib/types
---

## Iteration 8 - Create WelcomeModal component (US-008)
- Verified US-008 was already completed in a previous commit (de6f33a or earlier)
- WelcomeModal.tsx already exists at frontend/src/components/features/onboarding/WelcomeModal.tsx
- Component features:
  - Gradient welcome heading "Welcome to SyncScript, {userName}!"
  - 3 feature highlight cards with icons (Database, Users, FileText)
  - Glass morphism aesthetic (bg-[#0F1115], border border-white/10, backdrop-blur-lg)
  - Responsive grid (3 cols desktop, 1 col mobile with md: breakpoint)
  - GradientButton CTA styled per Bitcoin DeFi design system
- PRD20.md already marked task as [x] complete
- Typecheck passes
- No commit needed (already in git history)
- Learnings for future iterations:
  - Always check git log and HEAD commit before implementing tasks
  - Use `git show HEAD:path/to/file` to verify file contents in latest commit
  - PRD may already have tasks marked complete from previous iterations
  - Pattern: Check git status AND git show HEAD before creating files
---

## Iteration 9 - Create PathSelection component (US-009)
- Created PathSelection.tsx with 3 path option cards
- Path options:
  - Guided: Sparkles icon, gradient from-[#EA580C] to-[#F7931A]
  - Demo: Compass icon, gradient from-[#F7931A] to-[#FFD600]
  - Skipped: SkipForward icon, gradient from-[#94A3B8] to-[#64748B]
- Card hover effects: -translate-y-2, border glow, shadow, icon scale-110
- Updates onboarding via updateOnboarding() with path, step, and completed fields
- Responsive grid: grid-cols-1 md:grid-cols-3
- Files changed: PathSelection.tsx (new), PRD20.md, progress20.txt
- Commit: 1f99500
- Typecheck passes
- Learnings for future iterations:
  - Use lucide-react icons that match semantic meaning (Sparkles=guided, Compass=explore)
  - Path selection sets different steps: guided-1, demo, complete (for skipped)
  - Skip path marks completed=true immediately
  - Group hover effects using group and group-hover: modifiers
  - Icon container uses gradient background matching path theme
  - CTA text revealed on hover with opacity transition
---
