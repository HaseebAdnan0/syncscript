## Iteration 1 - US-001: Add WebSocket dependencies to requirements.txt
- Verified all required packages already present in requirements.txt:
  - channels[daphne]>=4.3.1
  - channels-redis>=4.2 (updated to >=4.2.0 for consistency)
  - redis>=5.0.1
  - hiredis>=2.3.2
- Files changed: backend/requirements.txt (minor version update)
- Verified packages install successfully
- All packages import correctly in Python
- Learnings for future iterations:
  - requirements.txt already had most dependencies committed
  - Pre-existing typecheck error exists with missing Vault model in vaults app - unrelated to WebSocket dependencies
  - Imports test confirms packages are functional
---

## Iteration 2 - US-002: Configure CHANNEL_LAYERS in Django settings
- Added 'channels' and 'daphne' to INSTALLED_APPS in settings.py
- Configured CHANNEL_LAYERS with RedisChannelLayer backend
  - Backend: 'channels_redis.core.RedisChannelLayer'
  - Hosts from REDIS_URL environment variable (defaults to redis://localhost:6379/0)
  - Capacity: 1500, Expiry: 10 seconds
- Added ASGI_APPLICATION = 'config.asgi.application'
- Files changed: backend/config/settings.py
- Verified configuration loads correctly with Django startup check
- Typecheck passed successfully
- Learnings for future iterations:
  - Settings.py auto-commits may happen via hooks - check git status after edits
  - Django settings can be tested with python -c import to verify configuration
  - REDIS_URL should follow format: redis://host:port/db
---

## Iteration 3 - US-003: Create ASGI application entry point
- Updated config/asgi.py to use ProtocolTypeRouter
  - HTTP protocol routes to django_asgi_app (get_asgi_application)
  - WebSocket protocol routes through AllowedHostsOriginValidator
  - Added placeholder comments for JWTAuthMiddleware and websocket_urlpatterns (to be implemented in later tasks)
- Files changed: backend/config/asgi.py
- Verified ASGI application loads successfully with both 'http' and 'websocket' protocol handlers
- Learnings for future iterations:
  - Django ASGI app must be initialized BEFORE importing code that uses ORM models
  - ProtocolTypeRouter requires dictionary mapping protocol names to handlers
  - Missing type stubs for channels is expected - don't need to install types-channels
  - Can verify ASGI application by importing and checking application_mapping.keys()
---
## Iteration 4 - US-004: Create WebSocket URL routing
- Created apps/vaults/routing.py with websocket_urlpatterns list
  - Route pattern: ws/vault/<int:vault_id>/ -> VaultConsumer
  - Used re_path from django.urls (standard Channels pattern)
- Updated config/asgi.py to import and use websocket_urlpatterns in URLRouter
- Created minimal VaultConsumer stub in apps/vaults/consumers.py
  - Placeholder implementation to satisfy routing import
  - Full implementation will be in US-006
- Files changed: backend/apps/vaults/routing.py, backend/apps/vaults/consumers.py, backend/config/asgi.py
- Verified routing loads successfully and ASGI application has both http and websocket protocols
- Typecheck passes with type: ignore comment for pyright/Channels compatibility
- Learnings for future iterations:
  - Channels URLRouter accepts standard Django URL patterns (re_path, path)
  - Pyright may flag ASGI consumer as invalid arg-type for re_path - use type: ignore[arg-type]
  - Creating minimal stubs for forward references is acceptable when dependencies are implemented in later tasks
  - Verify routing with: from apps.vaults.routing import websocket_urlpatterns
  - AsyncWebsocketConsumer.disconnect parameter must be named 'code' not 'close_code'
---
## Iteration 5 - US-005: Implement JWT authentication middleware for WebSockets
- Created apps/vaults/middleware.py with JWTAuthMiddleware class
  - Extends BaseMiddleware from channels.middleware
  - Extracts JWT token from query string parameter (?token=xxx)
  - Validates token using rest_framework_simplejwt.tokens.AccessToken
  - Sets scope["user"] to authenticated User or AnonymousUser
  - Handles TokenError, User.DoesNotExist, and generic exceptions gracefully
- Updated config/asgi.py to wrap URLRouter with JWTAuthMiddleware
- Files changed: backend/apps/vaults/middleware.py (new), backend/config/asgi.py, PRD5.md
- Verified middleware imports successfully and ASGI application loads
- Tested token handling: empty, invalid, and malformed tokens all return AnonymousUser
- Typecheck passes (0 errors, 0 warnings)
- Learnings for future iterations:
  - BaseMiddleware.__call__ signature requires type: ignore[override] and type: ignore[arg-type] for Pyright compatibility
  - Must use django.setup() before importing models in standalone test scripts
  - AccessToken constructor needs type: ignore[arg-type] annotation
  - Use type: ignore[import-not-found] for local app imports, type: ignore[import-untyped] for third-party untyped packages
  - Parse query string with urllib.parse.parse_qs, returns dict[str, list[str]]
  - database_sync_to_async decorator enables async database queries in middleware
---
## Iteration 6 - US-007: Implement room join/leave with channel layer
- Discovered US-007 implementation already present in backend/apps/vaults/consumers.py from previous commit
- Updated PRD5.md to mark US-007 as complete
- Verified implementation includes:
  - group_add on successful connect
  - group_discard on disconnect
  - connection.success message with sequence number (placeholder seq=0)
  - vault_event method for forwarding group messages to WebSocket clients
- Files changed: PRD5.md
- Typecheck passes (0 errors, 0 warnings)
- Learnings for future iterations:
  - Code implementation may exist from previous commits even if PRD task not marked complete
  - Always check git log and diff to understand what's already committed
  - channel_layer.group_add and group_discard require type: ignore[union-attr] for Pyright
  - vault_event method name matches Channels convention for handling group messages
  - Sequence number tracking will be implemented with Redis in later tasks (US-016/US-017)
---
## Iteration 7 - US-008: Implement presence tracking with Redis sorted set
- Implemented Redis-based presence tracking for vault collaborators
- Added _add_to_presence method to add users to sorted set on connect
- Added _remove_from_presence method to remove users on disconnect
- Created _get_presence_list helper function to fetch presence with user details
- Presence status calculated: 'active' (<60s), 'idle' (60-300s)
- User data enriched with username from database
- Files changed: backend/apps/vaults/consumers.py, PRD5.md
- Code committed in 47b2c6e (presence methods) and 1703c68 (PRD update)
- Learnings for future iterations:
  - Redis sorted sets (zadd, zrem, zrange) perfect for time-based presence tracking
  - Use cache.client.get_client() to access raw Redis connection
  - Score in sorted set represents timestamp for calculating user status
  - Database queries in async context require @database_sync_to_async decorator
  - Clean up stale presence entries when user no longer exists (User.DoesNotExist)
  - Auto-commit hooks may commit changes during file edits
---
## Iteration 8 - US-009: Broadcast presence updates on join/leave
- Implemented _broadcast_presence_update method in VaultConsumer
- Method calls _get_presence_list to fetch current active users
- Broadcasts presence.update event via channel_layer.group_send
- Presence update sent after user joins room (in connect method)
- Presence update sent after user leaves room (in disconnect method)
- Payload includes active_users array with user_id, username, joined_at, status
- Files changed: backend/apps/vaults/consumers.py, PRD5.md
- Typecheck passes (0 errors, 0 warnings)
- Learnings for future iterations:
  - channel_layer.group_send requires 'type' field that maps to consumer method name
  - vault_event method receives group messages and forwards to WebSocket clients
  - Presence broadcasts happen asynchronously via channel layer
  - Added type: ignore[attr-defined] for user.username to satisfy Pyright
  - Broadcast happens AFTER user is added/removed from presence sorted set
---
## Iteration 9 - US-010: Implement heartbeat handling
- Implemented receive method in VaultConsumer to handle incoming WebSocket messages
- Added support for {"type": "heartbeat"} messages from clients
- Created _update_presence method to update user timestamp in Redis sorted set
- Heartbeat updates timestamp without broadcasting (internal tracking only)
- Files changed: backend/apps/vaults/consumers.py, PRD5.md
- Created test_heartbeat.py to verify functionality (all tests passed)
- Typecheck passes (0 errors, 0 warnings)
- Learnings for future iterations:
  - receive method signature must match base class: text_data and bytes_data parameters with defaults
  - Early return on None text_data prevents unnecessary processing
  - JSON decode errors should be caught and logged but not crash the connection
  - Unknown message types should be logged as warnings for debugging
  - _update_presence reuses same Redis zadd logic as _add_to_presence (updates score)
  - Windows console doesn't support Unicode checkmarks - use [OK] prefix instead
---
## Iteration 10 - US-011: Create websocket_utils with broadcast helper
- Created core/websocket_utils.py with broadcast_to_vault function
- Function increments sequence number in Redis using redis_client.incr(vault_{id}:seq)
- Builds message envelope with:
  - type, seq, event: event_type
  - payload: provided dict
  - metadata: timestamp (timezone-aware), vault_id, optional user_id/username
- Sends to channel layer via group_send with type="vault_event"
- Files changed: backend/core/__init__.py (new), backend/core/websocket_utils.py (new), PRD5.md
- Already committed in 8bd6247 (auto-commit hook)
- Typecheck passes (0 errors, 0 warnings)
- Learnings for future iterations:
  - Created core/ directory structure - first shared utility module outside apps/
  - Use datetime.now(timezone.utc) instead of deprecated datetime.utcnow()
  - channel_layer.group_send requires 'type' field that maps to consumer method name (vault_event)
  - Message envelope wraps actual event data in consistent format for all broadcasts
  - async_to_sync wrapper needed when calling async channel layer from sync context (Celery tasks)
  - Redis sequence numbers provide monotonically increasing event ordering
  - Auto-commit hooks may commit files during edits - check git log to find commits
---

## Iteration 11 - US-012: Implement Source signal handlers
- Created apps/sources/signals.py with post_save and post_delete signal handlers
  - post_save handler enqueues broadcast_source_created (created=True) or broadcast_source_updated (created=False)
  - post_delete handler enqueues broadcast_source_deleted with minimal info (source_id, vault_id, deleted_by_id)
  - All signals use Celery .delay() for async broadcasting to avoid blocking HTTP requests
- Created apps/sources/apps.py with SourcesConfig.ready() to register signals
- Updated apps/sources/__init__.py with default_app_config
- Added stub Celery tasks to apps/sources/tasks.py (full implementation in US-013):
  - broadcast_source_created(source_id)
  - broadcast_source_updated(source_id, changed_fields)
  - broadcast_source_deleted(source_id, vault_id, deleted_by_id)
- Files changed: backend/apps/sources/signals.py (new), apps.py (new), __init__.py, tasks.py, PRD5.md
- Verified signal registration: 1 post_save receiver, 1 post_delete receiver for Source model
- Typecheck passes (0 errors, 0 warnings)
- Learnings for future iterations:
  - Signal handlers should import Celery tasks inside handler function to avoid circular imports
  - Use type: ignore[attr-defined] for .delay() method - Celery adds it at runtime
  - Creating stub tasks for forward references allows typecheck to pass before full implementation
  - Apps must define AppConfig.ready() to register signals on Django startup
  - Verify signal registration with: post_save._live_receivers(Model)
  - Windows console Unicode errors: use [OK] instead of checkmarks
  - Changed fields tracking can be added via model save() override or middleware
---

## Iteration 12 - US-013: Implement Source broadcast Celery tasks
- Implemented three Celery tasks for broadcasting Source model events:
  - broadcast_source_created(source_id): Fetches source with select_related, builds payload with id/url/title/created_by, broadcasts source.created event
  - broadcast_source_updated(source_id, changed_fields): Similar to created, includes changed_fields list, broadcasts source.updated event
  - broadcast_source_deleted(source_id, vault_id, deleted_by_id): Uses vault_id directly (source already deleted), fetches deleted_by user if provided
- All tasks use broadcast_to_vault helper from core.websocket_utils
- Payloads match event catalog: id, url, title, source_type, description, created_by{id, username}, timestamps
- Error handling: try/except for Source.DoesNotExist and User.DoesNotExist with logging
- Files changed: backend/apps/sources/tasks.py, PRD5.md
- Typecheck passes (only pre-existing pdf2image errors remain)
- Learnings for future iterations:
  - Import models inside task functions to avoid circular imports
  - Use select_related('created_by', 'vault') for efficient database queries
  - Type annotations: payload: dict[str, Any] prevents Pyright dict assignment errors
  - type: ignore[import-not-found] for local app imports, type: ignore[attr-defined] for Django model attributes
  - For deleted objects, pass vault_id as parameter since source no longer exists in database
  - User.DoesNotExist should be caught separately when fetching deleted_by user
  - Consistent logging: logger.info for success, logger.error for not found
---
## Iteration 13 - US-014: Implement Annotation signal handlers and tasks
- Discovered US-014 implementation already complete from previous commits
- Verified all components in place:
  - apps/annotations/models.py with Annotation model (id, source, text, page_number, created_by, timestamps)
  - apps/annotations/signals.py with post_save handler for created=True only
  - apps/annotations/tasks.py with broadcast_annotation_created Celery task
  - apps/annotations/apps.py with ready() method to register signals
  - apps/annotations/migrations/0001_initial.py migration created
  - Added apps.annotations to INSTALLED_APPS in settings.py
- Signal registration verified: 2 post_save receivers for Annotation model
- Typecheck passes for annotations app (0 errors)
- Payload includes: id, source_id, text, page_number, created_by{id, username}, created_at
- Files changed: All annotations app files already committed
- Learnings for future iterations:
  - Auto-commit hooks may commit entire app structure during development
  - Check git log and git show to verify what's already committed before implementing
  - Django auto-creates 'id' field, but add type annotation (id: int) for Pyright compatibility
  - Annotation model uses simple integer PK (not UUID like PDFUpload)
  - Signal handler only broadcasts on created=True, not on updates
  - Migration can be created even if database doesn't exist (makemigrations works offline)
  - Verify signal registration with: post_save._live_receivers(Model)
---
## Iteration 14 - US-015: Implement VaultMembership signal handlers and tasks
- Updated apps/vaults/signals.py with new post_save signal handler for WebSocket broadcasting
  - Added broadcast_membership_added receiver (created=True only)
  - Enqueues broadcast_member_added Celery task
  - Imports task inside handler to avoid circular imports
- Created apps/vaults/tasks.py with broadcast_member_added Celery task
  - Accepts membership_id parameter (UUID string)
  - Fetches VaultMembership with select_related for user, vault, added_by
  - Builds payload with member details: user_id, username, role, added_at
  - Includes added_by info when available (user_id and username)
  - Broadcasts member.added event to vault room via broadcast_to_vault
  - Error handling for VaultMembership.DoesNotExist
- Files changed: backend/apps/vaults/signals.py, backend/apps/vaults/tasks.py (new), PRD5.md
- Verification tests created (test_us015.py) - all pass:
  - Signal registration: 2 post_save receivers for VaultMembership
  - Task imports successfully
  - Task signature has membership_id parameter
  - Payload includes all required fields
- Typecheck passes (0 errors, 0 warnings)
- Committed in 5a8956f
- Learnings for future iterations:
  - VaultMembership already had log_membership_added signal - added second receiver for broadcasting
  - Created first tasks.py file in vaults app (sources and annotations already have tasks.py)
  - Signal handler pattern: enqueue Celery task with .delay() for async processing
  - Payload structure follows event catalog: member object with nested added_by when present
  - Use select_related('user', 'vault', 'added_by') for efficient queries
  - Test verification: check signal receiver count instead of parsing names
  - VaultMembership uses UUID primary key (id field)
---
