# Progress Log - PRD 19: Notification System

## Current Status
In Progress - US-004 complete

## Learnings
- Notification model was already created in a previous session
- Model includes all required fields: user FK, type, title, body, data (JSON), read_at, created_at
- Indexes added for efficient querying on user+read_at, user+created_at, created_at
- Type annotations required for mypy typecheck compliance
- Signal handlers need `del sender, kwargs` to avoid unused variable warnings
- OneToOneField requires related_name for reverse lookups
- post_save signals work correctly for auto-creating related models

---

## Iteration 1 - US-001: Notification Model
- Verified Notification model exists with all required fields
- Confirmed migration 0001_initial was already generated and applied
- Verified admin registration
- Typecheck passes with proper type annotations
- Files: backend/apps/notifications/models.py, admin.py, apps.py
- Learnings: App was previously created, task verification successful
---

## Iteration 2 - US-002: NotificationPreferences Model
- Added NotificationPreferences model with OneToOneField to User
- Implemented all boolean fields with correct defaults
- Added email_digest_frequency CharField with 4 choices
- Created post_save signal to auto-create preferences on user creation
- Registered model in admin with relevant filters
- Generated and applied migration 0002_notificationpreferences
- Tested signal - confirmed preferences auto-created for new users
- Mypy typecheck passes
- Files changed: backend/apps/notifications/models.py, admin.py, migrations/0002_notificationpreferences.py
- Learnings for future iterations:
  - Signal receiver functions need type: ignore[misc] annotation for Django signals
  - Use `del sender, kwargs` to mark unused but required signal parameters
  - OneToOneField creates reverse accessor via related_name
  - Test signals with simple user creation, avoid complex cleanup operations
---

## Iteration 3 - US-003: MutedVault Model (Verification)
- Task was already completed in previous session (commit bf8a79e)
- Verified MutedVault model exists with all required fields
- Confirmed migration 0003_mutedvault exists and is applied
- Verified admin registration with search and filters
- Mypy typecheck passes
- Files: backend/apps/notifications/models.py, admin.py, migrations/0003_mutedvault.py
- Learnings: Always check git history when task appears complete
---

## Iteration 4 - US-004: Notification Serializers
- Created `backend/apps/notifications/serializers.py` with three serializers
- NotificationSerializer: includes all fields plus is_read computed property via SerializerMethodField
- NotificationPreferencesSerializer: validates email_digest_frequency choices
- MutedVaultSerializer: includes vault_id and vault_name from related Vault model
- Followed established DRF patterns from vaults/serializers.py
- Mypy typecheck passes (Pyright warnings are standard DRF Meta class issues)
- Files changed: backend/apps/notifications/serializers.py, PRD19.md
- Learnings for future iterations:
  - SerializerMethodField naming: get_{field_name} method convention
  - Use source= parameter to access related model fields for read-only display
  - validate_{field_name} methods for field-level validation
  - read_only_fields in Meta prevents modification of system-managed fields
---

## Story Progress

### Backend Foundation
- [x] US-001: Notification model
- [x] US-002: NotificationPreferences model
- [x] US-003: MutedVault model
- [x] US-004: Serializers

### Backend API
- [ ] US-005: List endpoint
- [ ] US-006: Mark read endpoint
- [ ] US-007: Mark all read endpoint
- [ ] US-008: Unread count endpoint
- [ ] US-009: Preferences endpoints
- [ ] US-010: Muted vaults endpoints

### Backend Services & Signals
- [ ] US-011: Notification helper service
- [ ] US-012: vault_invite signal
- [ ] US-013: member_joined signal
- [ ] US-014: source_added signal
- [ ] US-015: annotation_reply signal
- [ ] US-016: @mention utility
- [ ] US-017: mention signal

### Real-time & Email
- [ ] US-018: Pusher integration
- [ ] US-019: Pusher auth endpoint
- [ ] US-020: Email service
- [ ] US-021: HTML email template
- [ ] US-022: Immediate email task
- [ ] US-023: Daily/weekly digest task
- [ ] US-024: Cleanup task

### Frontend Core
- [ ] US-025: Types & API client
- [ ] US-026: NotificationBell component
- [ ] US-027: NotificationItem component
- [ ] US-028: NotificationDropdown component
- [ ] US-029: useNotifications hook
- [ ] US-030: Header integration

### Frontend Real-time
- [ ] US-031: Pusher client setup
- [ ] US-032: usePusherNotifications hook
- [ ] US-033: Toast system
- [ ] US-034: Toast + Pusher integration
- [ ] US-035: Click navigation

### Frontend Preferences
- [ ] US-036: Preferences page layout
- [ ] US-037: Email toggles
- [ ] US-038: Email frequency selector
- [ ] US-039: Push notification controls
- [ ] US-040: Muted vaults management
- [ ] US-041: Mute vault button

### Mentions UI
- [ ] US-042: MentionAutocomplete component
- [ ] US-043: Annotation editor integration

### Testing
- [ ] US-044: Comprehensive tests

---

## Iteration 5 - US-005: Notification List Endpoint
- Implemented NotificationViewSet with ReadOnlyModelViewSet
- GET /api/v1/notifications/ returns paginated notifications (20 per page)
- Filters to authenticated user only
- Ordering: unread first using F('read_at').asc(nulls_first=True), then by -created_at
- Support ?unread_only=true query parameter
- Added mark_as_read() method to Notification model for tests
- Created comprehensive test suite (6 test cases, all passing)
- Created notifications URLs and registered in config/urls.py
- Fixed import issue in sources.services.__init__.py
- All tests pass, mypy typecheck passes
- Files changed: 
  - backend/apps/notifications/views.py
  - backend/apps/notifications/urls.py
  - backend/apps/notifications/models.py
  - backend/apps/notifications/tests.py
  - backend/config/urls.py
  - PRD19.md
- Learnings for future iterations:
  - Use F('read_at').asc(nulls_first=True) for PostgreSQL NULL ordering (unread first)
  - Default order_by('read_at') puts NULL last in PostgreSQL
  - ReadOnlyModelViewSet for list/retrieve-only endpoints
  - Custom pagination class: inherit PageNumberPagination, set page_size
  - Test response.data needs # type: ignore[attr-defined] for mypy
  - Query param filtering: self.request.query_params.get('param') == 'value'
---


## Iteration 6 - US-006: Mark Notification as Read Endpoint
- Implemented mark_read custom action in NotificationViewSet
- Added PATCH /api/v1/notifications/{id}/read/ endpoint
- Idempotent: only sets read_at on first call, subsequent calls preserve timestamp
- Security: only allows marking own notifications (404 for others via get_object())
- Returns updated notification with serializer
- Created comprehensive test suite with 5 test cases:
  - Test marking notification as read
  - Test idempotency (timestamp preservation)
  - Test ownership validation (404 for other user's notifications)
  - Test response includes updated data
  - Test authentication required
- All tests pass (5/5), mypy typecheck passes
- Files changed:
  - backend/apps/notifications/views.py (added mark_read action)
  - backend/apps/notifications/tests.py (added MarkNotificationReadTestCase)
  - PRD19.md (marked US-006 complete)
- Learnings for future iterations:
  - Use @action(detail=True) for single resource actions
  - get_object() automatically enforces queryset filtering (user ownership)
  - Idempotency pattern: check if field is None before updating
  - PATCH is semantic for partial updates like marking read
  - 404 is correct response when object doesn't exist in user's queryset (not 403)
---

## Iteration 7 - US-007: Mark All Notifications as Read Endpoint
- Verified mark_all_read action already implemented in NotificationViewSet
- POST /api/v1/notifications/mark-all-read/ uses efficient bulk update
- Returns count of updated notifications in response
- Comprehensive tests already exist: 5 test cases covering all scenarios
  - Empty state returns 0
  - Marks all unread notifications
  - Only updates unread (doesn't touch already read)
  - Filters by authenticated user
  - Requires authentication
- All tests pass (5/5), mypy typecheck passes
- Files changed:
  - PRD19.md (marked US-007 complete)
- Learnings for future iterations:
  - Use .update() for bulk operations instead of iterating (more efficient)
  - .update() returns count of affected rows automatically
  - Efficient query: filter(read_at__isnull=True).update(read_at=timezone.now())
  - No need to refresh individual objects when using bulk update
  - Tests should verify count returned matches expected updates
---

## Iteration 8 - US-008: Unread Count Endpoint (Verification)
- Verified unread_count endpoint already implemented in NotificationViewSet
- GET /api/v1/notifications/unread-count/ returns { "count": N }
- Uses efficient COUNT query: .filter(user=user, read_at__isnull=True).count()
- Comprehensive test suite already exists with 5 test cases:
  - Empty state returns 0
  - Counts unread notifications correctly
  - Excludes read notifications
  - Filters by authenticated user
  - Requires authentication
- All tests pass (5/5), mypy typecheck passes
- Files changed: PRD19.md (marked US-008 complete)
- Learnings for future iterations:
  - Use .count() for efficient counting without fetching all objects
  - Filter with read_at__isnull=True to identify unread notifications
  - Always verify if endpoint already exists before implementing
  - Comprehensive tests should cover edge cases: empty, partial, full, user isolation, auth
---
## Iteration 9 - US-009: Notification Preferences Endpoints
- Implemented GET and PATCH /api/v1/notifications/preferences/ endpoints
- GET auto-creates preferences using get_or_create if missing
- PATCH supports partial updates with partial=True
- Added comprehensive test suite with 6 test cases:
  - Get preferences returns all fields with default values
  - Auto-creates preferences if missing on GET
  - PATCH updates specified fields in database
  - Validates email_digest_frequency choices (400 on invalid)
  - Partial updates preserve unchanged fields
  - Requires authentication
- All tests pass (6/6), mypy typecheck passes
- Files changed:
  - backend/apps/notifications/views.py (added preferences action)
  - backend/apps/notifications/tests.py (added NotificationPreferencesTestCase)
  - PRD19.md (marked US-009 complete)
- Learnings for future iterations:
  - Use get_or_create() for auto-creating related models on first access
  - @action(detail=False) with methods=['get', 'patch'] for single resource update
  - partial=True in serializer enables PATCH-style partial updates
  - Validation in serializer (validate_field_name) runs automatically
  - PATCH should handle both GET and PATCH in same action method
---

## Iteration 10 - US-010: Muted Vaults Endpoints
- Implemented GET /api/v1/notifications/muted-vaults/ to list muted vaults
- Implemented POST /api/v1/notifications/muted-vaults/ to mute a vault with vault_id validation
- Implemented DELETE /api/v1/notifications/muted-vaults/{vault_id}/ to unmute a vault
- Validates user is a member of the vault before allowing mute operation (403 if not)
- Validates vault exists (404 if not found)
- Idempotent operations: mute returns 201 on create, 200 on existing; unmute always returns 204
- Fixed MutedVaultSerializer to use UUIDField instead of IntegerField for vault_id
- Created comprehensive test suite with 11 test cases covering all scenarios:
  - Empty list, mute vault, idempotent mute, vault_id required
  - Membership validation, vault exists validation
  - Get muted vaults with details, unmute vault, idempotent unmute
  - User filtering, authentication required
- Fixed setUp to avoid duplicate VaultMembership creation (owner membership auto-created by signal)
- Fixed variable naming conflict in views (muted vs muted_vaults)
- All tests pass (11/11), mypy typecheck passes
- Files changed:
  - backend/apps/notifications/views.py (added muted_vaults and unmute_vault actions)
  - backend/apps/notifications/serializers.py (fixed vault_id to UUIDField)
  - backend/apps/notifications/tests.py (added MutedVaultsTestCase with 11 tests)
  - PRD19.md (marked US-010 complete)
- Learnings for future iterations:
  - Vault.objects.create(owner=user) auto-creates VaultMembership via signal (don't create manually)
  - UUIDs are serialized as strings in JSON, compare with str() in tests
  - Use get_or_create for idempotent create operations, check 'created' boolean for status code
  - Variable names in same function scope must be unique for mypy
  - Import models locally in views to avoid circular imports
  - Custom URL path patterns: @action(detail=False, url_path='muted-vaults/(?P<vault_id>[^/.]+)')
  - DELETE endpoints should be idempotent (204 even if resource doesn't exist)
---

## Iteration 11 - US-011: Create Notification Helper Service
- Created apps/notifications/services.py with create_notification() function
- Implements vault muting check: skip notification if vault_id in data and vault is muted
- Implements user preference checks:
  - vault_invite: always sent (no preference to disable)
  - member_joined, source_added: check email_vault_activity
  - annotation_reply, mention: check email_mentions
- Auto-creates NotificationPreferences using get_or_create if missing
- Returns created Notification or None if skipped due to mute/preferences
- Added comprehensive test suite with 7 test cases:
  - Successful notification creation
  - Muted vault skipping
  - Preference-based filtering (vault_activity, mentions)
  - Vault invites always sent regardless of preferences
  - Notifications without vault_id
  - Auto-creation of preferences
- Fixed unrelated import error: added missing AskQuestionSerializer to apps.ai.serializers
- All tests pass (46/46), commit dd3eed8
- Files changed:
  - backend/apps/notifications/services.py (new)
  - backend/apps/notifications/tests.py (added NotificationServiceTestCase)
  - backend/apps/ai/serializers.py (fixed import error)
  - PRD19.md (marked US-011 complete)
- Learnings for future iterations:
  - Service functions should be stateless and pure (no side effects beyond creating notification)
  - Use get_or_create() to handle missing related objects gracefully
  - Preference checking logic should be explicit per notification type
  - Return None for skipped notifications to allow callers to track skip reasons
  - Test both positive and negative cases (create vs skip) for each condition
  - Always fix blocking import errors before running tests
---
