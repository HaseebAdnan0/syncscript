# Progress Log - PRD 19: Notification System

## Current Status
In Progress - US-004 complete

## Learnings
- Notification model was already created in a previous session
- Model includes all required fields: user FK, type, title, body, data (JSON), read_at, created_at
- Indexes added for efficient querying on user+read_at, user+created_at, created_at
- Type annotations required for mypy typecheck compliance
- Signal handlers need `del sender, kwargs` to avoid unused variable warnings
- OneToOneField requires related_name for reverse lookups
- post_save signals work correctly for auto-creating related models

---

## Iteration 1 - US-001: Notification Model
- Verified Notification model exists with all required fields
- Confirmed migration 0001_initial was already generated and applied
- Verified admin registration
- Typecheck passes with proper type annotations
- Files: backend/apps/notifications/models.py, admin.py, apps.py
- Learnings: App was previously created, task verification successful
---

## Iteration 2 - US-002: NotificationPreferences Model
- Added NotificationPreferences model with OneToOneField to User
- Implemented all boolean fields with correct defaults
- Added email_digest_frequency CharField with 4 choices
- Created post_save signal to auto-create preferences on user creation
- Registered model in admin with relevant filters
- Generated and applied migration 0002_notificationpreferences
- Tested signal - confirmed preferences auto-created for new users
- Mypy typecheck passes
- Files changed: backend/apps/notifications/models.py, admin.py, migrations/0002_notificationpreferences.py
- Learnings for future iterations:
  - Signal receiver functions need type: ignore[misc] annotation for Django signals
  - Use `del sender, kwargs` to mark unused but required signal parameters
  - OneToOneField creates reverse accessor via related_name
  - Test signals with simple user creation, avoid complex cleanup operations
---

## Iteration 3 - US-003: MutedVault Model (Verification)
- Task was already completed in previous session (commit bf8a79e)
- Verified MutedVault model exists with all required fields
- Confirmed migration 0003_mutedvault exists and is applied
- Verified admin registration with search and filters
- Mypy typecheck passes
- Files: backend/apps/notifications/models.py, admin.py, migrations/0003_mutedvault.py
- Learnings: Always check git history when task appears complete
---

## Iteration 4 - US-004: Notification Serializers
- Created `backend/apps/notifications/serializers.py` with three serializers
- NotificationSerializer: includes all fields plus is_read computed property via SerializerMethodField
- NotificationPreferencesSerializer: validates email_digest_frequency choices
- MutedVaultSerializer: includes vault_id and vault_name from related Vault model
- Followed established DRF patterns from vaults/serializers.py
- Mypy typecheck passes (Pyright warnings are standard DRF Meta class issues)
- Files changed: backend/apps/notifications/serializers.py, PRD19.md
- Learnings for future iterations:
  - SerializerMethodField naming: get_{field_name} method convention
  - Use source= parameter to access related model fields for read-only display
  - validate_{field_name} methods for field-level validation
  - read_only_fields in Meta prevents modification of system-managed fields
---

## Story Progress

### Backend Foundation
- [x] US-001: Notification model
- [x] US-002: NotificationPreferences model
- [x] US-003: MutedVault model
- [x] US-004: Serializers

### Backend API
- [ ] US-005: List endpoint
- [ ] US-006: Mark read endpoint
- [ ] US-007: Mark all read endpoint
- [ ] US-008: Unread count endpoint
- [ ] US-009: Preferences endpoints
- [ ] US-010: Muted vaults endpoints

### Backend Services & Signals
- [ ] US-011: Notification helper service
- [ ] US-012: vault_invite signal
- [ ] US-013: member_joined signal
- [ ] US-014: source_added signal
- [ ] US-015: annotation_reply signal
- [ ] US-016: @mention utility
- [ ] US-017: mention signal

### Real-time & Email
- [ ] US-018: Pusher integration
- [ ] US-019: Pusher auth endpoint
- [ ] US-020: Email service
- [ ] US-021: HTML email template
- [ ] US-022: Immediate email task
- [ ] US-023: Daily/weekly digest task
- [ ] US-024: Cleanup task

### Frontend Core
- [ ] US-025: Types & API client
- [ ] US-026: NotificationBell component
- [ ] US-027: NotificationItem component
- [ ] US-028: NotificationDropdown component
- [ ] US-029: useNotifications hook
- [ ] US-030: Header integration

### Frontend Real-time
- [ ] US-031: Pusher client setup
- [ ] US-032: usePusherNotifications hook
- [ ] US-033: Toast system
- [ ] US-034: Toast + Pusher integration
- [ ] US-035: Click navigation

### Frontend Preferences
- [ ] US-036: Preferences page layout
- [ ] US-037: Email toggles
- [ ] US-038: Email frequency selector
- [ ] US-039: Push notification controls
- [ ] US-040: Muted vaults management
- [ ] US-041: Mute vault button

### Mentions UI
- [ ] US-042: MentionAutocomplete component
- [ ] US-043: Annotation editor integration

### Testing
- [ ] US-044: Comprehensive tests

---

## Iteration 5 - US-005: Notification List Endpoint
- Implemented NotificationViewSet with ReadOnlyModelViewSet
- GET /api/v1/notifications/ returns paginated notifications (20 per page)
- Filters to authenticated user only
- Ordering: unread first using F('read_at').asc(nulls_first=True), then by -created_at
- Support ?unread_only=true query parameter
- Added mark_as_read() method to Notification model for tests
- Created comprehensive test suite (6 test cases, all passing)
- Created notifications URLs and registered in config/urls.py
- Fixed import issue in sources.services.__init__.py
- All tests pass, mypy typecheck passes
- Files changed: 
  - backend/apps/notifications/views.py
  - backend/apps/notifications/urls.py
  - backend/apps/notifications/models.py
  - backend/apps/notifications/tests.py
  - backend/config/urls.py
  - PRD19.md
- Learnings for future iterations:
  - Use F('read_at').asc(nulls_first=True) for PostgreSQL NULL ordering (unread first)
  - Default order_by('read_at') puts NULL last in PostgreSQL
  - ReadOnlyModelViewSet for list/retrieve-only endpoints
  - Custom pagination class: inherit PageNumberPagination, set page_size
  - Test response.data needs # type: ignore[attr-defined] for mypy
  - Query param filtering: self.request.query_params.get('param') == 'value'
---


## Iteration 6 - US-006: Mark Notification as Read Endpoint
- Implemented mark_read custom action in NotificationViewSet
- Added PATCH /api/v1/notifications/{id}/read/ endpoint
- Idempotent: only sets read_at on first call, subsequent calls preserve timestamp
- Security: only allows marking own notifications (404 for others via get_object())
- Returns updated notification with serializer
- Created comprehensive test suite with 5 test cases:
  - Test marking notification as read
  - Test idempotency (timestamp preservation)
  - Test ownership validation (404 for other user's notifications)
  - Test response includes updated data
  - Test authentication required
- All tests pass (5/5), mypy typecheck passes
- Files changed:
  - backend/apps/notifications/views.py (added mark_read action)
  - backend/apps/notifications/tests.py (added MarkNotificationReadTestCase)
  - PRD19.md (marked US-006 complete)
- Learnings for future iterations:
  - Use @action(detail=True) for single resource actions
  - get_object() automatically enforces queryset filtering (user ownership)
  - Idempotency pattern: check if field is None before updating
  - PATCH is semantic for partial updates like marking read
  - 404 is correct response when object doesn't exist in user's queryset (not 403)
---

## Iteration 7 - US-007: Mark All Notifications as Read Endpoint
- Verified mark_all_read action already implemented in NotificationViewSet
- POST /api/v1/notifications/mark-all-read/ uses efficient bulk update
- Returns count of updated notifications in response
- Comprehensive tests already exist: 5 test cases covering all scenarios
  - Empty state returns 0
  - Marks all unread notifications
  - Only updates unread (doesn't touch already read)
  - Filters by authenticated user
  - Requires authentication
- All tests pass (5/5), mypy typecheck passes
- Files changed:
  - PRD19.md (marked US-007 complete)
- Learnings for future iterations:
  - Use .update() for bulk operations instead of iterating (more efficient)
  - .update() returns count of affected rows automatically
  - Efficient query: filter(read_at__isnull=True).update(read_at=timezone.now())
  - No need to refresh individual objects when using bulk update
  - Tests should verify count returned matches expected updates
---

## Iteration 8 - US-008: Unread Count Endpoint (Verification)
- Verified unread_count endpoint already implemented in NotificationViewSet
- GET /api/v1/notifications/unread-count/ returns { "count": N }
- Uses efficient COUNT query: .filter(user=user, read_at__isnull=True).count()
- Comprehensive test suite already exists with 5 test cases:
  - Empty state returns 0
  - Counts unread notifications correctly
  - Excludes read notifications
  - Filters by authenticated user
  - Requires authentication
- All tests pass (5/5), mypy typecheck passes
- Files changed: PRD19.md (marked US-008 complete)
- Learnings for future iterations:
  - Use .count() for efficient counting without fetching all objects
  - Filter with read_at__isnull=True to identify unread notifications
  - Always verify if endpoint already exists before implementing
  - Comprehensive tests should cover edge cases: empty, partial, full, user isolation, auth
---
## Iteration 9 - US-009: Notification Preferences Endpoints
- Implemented GET and PATCH /api/v1/notifications/preferences/ endpoints
- GET auto-creates preferences using get_or_create if missing
- PATCH supports partial updates with partial=True
- Added comprehensive test suite with 6 test cases:
  - Get preferences returns all fields with default values
  - Auto-creates preferences if missing on GET
  - PATCH updates specified fields in database
  - Validates email_digest_frequency choices (400 on invalid)
  - Partial updates preserve unchanged fields
  - Requires authentication
- All tests pass (6/6), mypy typecheck passes
- Files changed:
  - backend/apps/notifications/views.py (added preferences action)
  - backend/apps/notifications/tests.py (added NotificationPreferencesTestCase)
  - PRD19.md (marked US-009 complete)
- Learnings for future iterations:
  - Use get_or_create() for auto-creating related models on first access
  - @action(detail=False) with methods=['get', 'patch'] for single resource update
  - partial=True in serializer enables PATCH-style partial updates
  - Validation in serializer (validate_field_name) runs automatically
  - PATCH should handle both GET and PATCH in same action method
---

## Iteration 10 - US-010: Muted Vaults Endpoints
- Implemented GET /api/v1/notifications/muted-vaults/ to list muted vaults
- Implemented POST /api/v1/notifications/muted-vaults/ to mute a vault with vault_id validation
- Implemented DELETE /api/v1/notifications/muted-vaults/{vault_id}/ to unmute a vault
- Validates user is a member of the vault before allowing mute operation (403 if not)
- Validates vault exists (404 if not found)
- Idempotent operations: mute returns 201 on create, 200 on existing; unmute always returns 204
- Fixed MutedVaultSerializer to use UUIDField instead of IntegerField for vault_id
- Created comprehensive test suite with 11 test cases covering all scenarios:
  - Empty list, mute vault, idempotent mute, vault_id required
  - Membership validation, vault exists validation
  - Get muted vaults with details, unmute vault, idempotent unmute
  - User filtering, authentication required
- Fixed setUp to avoid duplicate VaultMembership creation (owner membership auto-created by signal)
- Fixed variable naming conflict in views (muted vs muted_vaults)
- All tests pass (11/11), mypy typecheck passes
- Files changed:
  - backend/apps/notifications/views.py (added muted_vaults and unmute_vault actions)
  - backend/apps/notifications/serializers.py (fixed vault_id to UUIDField)
  - backend/apps/notifications/tests.py (added MutedVaultsTestCase with 11 tests)
  - PRD19.md (marked US-010 complete)
- Learnings for future iterations:
  - Vault.objects.create(owner=user) auto-creates VaultMembership via signal (don't create manually)
  - UUIDs are serialized as strings in JSON, compare with str() in tests
  - Use get_or_create for idempotent create operations, check 'created' boolean for status code
  - Variable names in same function scope must be unique for mypy
  - Import models locally in views to avoid circular imports
  - Custom URL path patterns: @action(detail=False, url_path='muted-vaults/(?P<vault_id>[^/.]+)')
  - DELETE endpoints should be idempotent (204 even if resource doesn't exist)
---

## Iteration 11 - US-011: Create Notification Helper Service
- Created apps/notifications/services.py with create_notification() function
- Implements vault muting check: skip notification if vault_id in data and vault is muted
- Implements user preference checks:
  - vault_invite: always sent (no preference to disable)
  - member_joined, source_added: check email_vault_activity
  - annotation_reply, mention: check email_mentions
- Auto-creates NotificationPreferences using get_or_create if missing
- Returns created Notification or None if skipped due to mute/preferences
- Added comprehensive test suite with 7 test cases:
  - Successful notification creation
  - Muted vault skipping
  - Preference-based filtering (vault_activity, mentions)
  - Vault invites always sent regardless of preferences
  - Notifications without vault_id
  - Auto-creation of preferences
- Fixed unrelated import error: added missing AskQuestionSerializer to apps.ai.serializers
- All tests pass (46/46), commit dd3eed8
- Files changed:
  - backend/apps/notifications/services.py (new)
  - backend/apps/notifications/tests.py (added NotificationServiceTestCase)
  - backend/apps/ai/serializers.py (fixed import error)
  - PRD19.md (marked US-011 complete)
- Learnings for future iterations:
  - Service functions should be stateless and pure (no side effects beyond creating notification)
  - Use get_or_create() to handle missing related objects gracefully
  - Preference checking logic should be explicit per notification type
  - Return None for skipped notifications to allow callers to track skip reasons
  - Test both positive and negative cases (create vs skip) for each condition
  - Always fix blocking import errors before running tests
---

## Iteration 12 - US-012: Signal Handler for vault_invite Notifications (Verification)
- Task was already completed in previous session (commit 4426df2)
- Verified signals.py exists with VaultMembership post_save handler
- Verified signal creates vault_invite notification when user added to vault
- Verified signal doesn't notify vault owner (auto-created membership)
- Verified notification data includes vault_id, vault_name, inviter_id, inviter_name
- Verified signal connected in apps.py ready() method
- Verified comprehensive test suite exists with 5 test cases
- All tests pass (51/51)
- Mypy typecheck passes
- Files: backend/apps/notifications/signals.py, apps.py, tests.py
- Learnings: Always check git history to verify if task already exists before reimplementing
---

## Iteration 13 - US-013: Signal Handler for member_joined Notifications
- Extended existing VaultMembership post_save signal handler to also create member_joined notifications
- Notify vault owner when someone joins their vault (if different from new member)
- Signal respects muted vaults and email_vault_activity preference via create_notification service
- Created comprehensive test suite with 5 test cases:
  - Test owner receives notification when someone joins
  - Test owner doesn't get notified when joining their own vault
  - Test respects muted vault preference
  - Test respects email_vault_activity preference
  - Test both vault_invite and member_joined notifications created simultaneously
- All tests pass (56/56)
- Files changed:
  - backend/apps/notifications/signals.py (extended vault_membership_created handler)
  - backend/apps/notifications/tests.py (added MemberJoinedSignalTestCase)
  - PRD19.md (marked US-013 complete)
- Learnings for future iterations:
  - Single signal handler can create multiple notifications for different users
  - Both vault_invite (for new member) and member_joined (for owner) happen on same VaultMembership creation
  - The condition `instance.vault.owner != instance.user` prevents owner from being notified when joining own vault
  - create_notification service automatically handles muted vault and preference checks
  - Pre-existing mypy errors in vaults/models.py are unrelated to notification changes
  - Tests validate that both notifications are created independently with correct data
---

## Iteration 14 - US-014: Signal Handler for source_added Notifications
- Created Source post_save signal handler to notify vault members when sources are added
- Notifies all vault members except the creator
- Signal respects muted vaults and email_vault_activity preference via create_notification service
- Loops through all vault members and creates individual notifications
- Created comprehensive test suite with 6 test cases:
  - Test all members (except creator) receive notifications
  - Test creator doesn't get notified about their own source
  - Test signal doesn't trigger on updates (only creation)
  - Test respects muted vault preference
  - Test respects email_vault_activity preference
  - Test handles unknown creator (created_by=None)
- All tests pass (62/62)
- Files changed:
  - backend/apps/notifications/signals.py (added source_created handler, imported Source)
  - backend/apps/notifications/test_source_signal.py (new test file)
  - PRD19.md (marked US-014 complete)
- Learnings for future iterations:
  - Single signal can notify multiple users via loop over vault.members.all()
  - Use instance.created_by.id comparison to skip notifying the creator
  - Handle None created_by gracefully with conditional check before comparison
  - Source.vault.members gives access to all vault members (User objects)
  - create_notification automatically handles muting and preferences per user
  - Separate test file is acceptable when test suite gets large
---

## Iteration 15 - US-015: Signal Handler for annotation_reply Notifications
- Created Annotation post_save signal handler to notify parent annotation authors of replies
- Only handles new annotations with parent set (replies)
- Prevents self-notification when replying to own annotation
- Truncates preview to 100 characters with "..." for long content
- Signal respects email_mentions preference via create_notification service
- Created comprehensive test suite with 6 test cases:
  - Test notification created when someone replies
  - Test preview truncated to 100 chars for long content
  - Test signal doesn't fire for top-level annotations (no parent)
  - Test no notification for self-replies
  - Test signal doesn't trigger on updates (only creation)
  - Test respects email_mentions preference
- All tests pass (68/68)
- Files changed:
  - backend/apps/notifications/signals.py (added annotation_created handler, imported Annotation)
  - backend/apps/notifications/test_annotation_signal.py (new test file)
  - PRD19.md (marked US-015 complete)
- Learnings for future iterations:
  - Check if instance.parent exists to identify replies vs top-level annotations
  - instance.parent.user gives access to the parent annotation's author
  - String slicing with [:100] + "..." pattern for preview truncation
  - annotation_reply uses email_mentions preference (not email_vault_activity)
  - Annotation model has self-referential ForeignKey for threading
  - Preview field useful for showing context in notification UI without full content
---

## Iteration 16 - US-016: Create @mention Detection Utility
- Created apps/notifications/mentions.py with two utility functions
- extract_mentions(text): Uses regex pattern (?:^|\s)@(\w+) to find @username mentions
  - Handles edge cases: start of text, after whitespace, after punctuation
  - Ignores email addresses (email@domain.com not treated as mention)
  - Supports usernames with underscores and numbers
  - Deduplicates mentions while preserving order
- resolve_mentions(usernames, vault_id): Resolves usernames to User objects
  - Filters by vault membership (only returns users who are vault members)
  - Ignores invalid/non-existent usernames silently
  - Handles invalid vault_id gracefully (returns empty list)
  - Automatically deduplicates via database query
- Created comprehensive test suite with 18 test cases:
  - Extract: single, multiple, at start, after whitespace, after punctuation
  - Extract: ignore emails, handle underscores/numbers, deduplicate, empty text
  - Resolve: valid members, ignore non-members, ignore invalid usernames
  - Resolve: empty list, invalid vault, all members, duplicates
- All tests pass (86/86)
- Files changed:
  - backend/apps/notifications/mentions.py (new utility module)
  - backend/apps/notifications/test_mentions.py (new test file)
  - PRD19.md (marked US-016 complete)
- Learnings for future iterations:
  - Regex pattern (?:^|\s)@(\w+) captures @ at start or after whitespace
  - re.findall() returns only captured groups (usernames without @)
  - Preserve order while deduplicating: use set to track seen + list for order
  - Import Vault locally in function to avoid circular imports
  - vault.members.filter(username__in=usernames) efficiently resolves multiple usernames
  - Type annotation List[User] requires # type: ignore[type-arg] for Django User model
---

## Iteration 13 - TIMEOUT
- Task timed out after 30 minutes
- Process was killed
---


## Iteration 17 - US-017: Signal Handler for Mention Notifications (Verification)
- Task was already completed in previous session
- Verified mention notification logic integrated into annotation_created signal handler (signals.py lines 195-239)
- Signal extracts @mentions using extract_mentions() from mentions.py
- Creates mention notification for each mentioned vault member via resolve_mentions()
- Prevents self-mentions (instance.user.id check)
- Prevents duplicate notifications if mentioned user is also annotation reply recipient
- Comprehensive test suite exists in test_mention_signal.py with 7 test cases:
  - Single and multiple mentions
  - Self-mention prevention
  - Duplicate prevention when mentioned user is parent annotation author
  - Respects email_mentions preference
  - Ignores non-vault members
  - Handles both annotation_reply and mention in same annotation
- All acceptance criteria met
- Files: backend/apps/notifications/signals.py, test_mention_signal.py, PRD19.md
- Learnings for future iterations:
  - Mention notification signal integrated into annotation_created handler (not separate handler)
  - already_notified_user_id tracks annotation_reply recipient to prevent duplicates
  - resolve_mentions() automatically filters to vault members only
  - Both annotation_reply and mention notifications can be created from same annotation
  - Always check existing signal handlers before creating new ones
---

## Iteration 18 - US-018: Integrate Pusher for Real-time Notification Delivery
- Integrated Pusher into create_notification() service to deliver real-time notifications
- Created lazy-loaded get_pusher_client() function to avoid initialization errors when Pusher not configured
- Implemented _send_pusher_notification() helper that triggers two events:
  - 'notification' event with serialized notification data (NotificationSerializer)
  - 'badge_update' event with current unread count
- Events sent to private channel: `private-user-{user_id}`
- Pusher client auto-created on first use with credentials from environment variables
- Silently fails if Pusher not configured (returns None) or if trigger raises exception
- Created comprehensive test suite with 6 test cases in test_pusher_integration.py:
  - Test notification and badge_update events triggered
  - Test badge count reflects correct unread count
  - Test badge count excludes read notifications
  - Test Pusher skipped when not configured
  - Test notification created even if Pusher fails
  - Test correct private channel format used
- All tests pass (6/6), commit 0bb5041
- Files changed:
  - backend/apps/notifications/services.py (added Pusher integration)
  - backend/apps/notifications/test_pusher_integration.py (new test file)
  - PRD19.md (marked US-018 complete)
- Learnings for future iterations:
  - Lazy-load external clients to avoid initialization errors at module import time
  - Use global variable with getter function pattern for singleton clients
  - Pusher requires valid app_id, can't be empty string (use None check instead)
  - Mock get_pusher_client in tests and return MagicMock for client
  - Wrap Pusher trigger in try/except to prevent blocking notification creation on errors
  - SerializerMethodField and serializer.data work seamlessly for Pusher payloads
  - Private channel format: `private-user-{user_id}` (will require auth endpoint later)
---

## Iteration 19 - US-019: Create Pusher Authentication Endpoint
- Implemented POST /api/v1/notifications/pusher/auth/ endpoint in NotificationViewSet
- Validates channel_name and socket_id are provided (400 if missing)
- Validates channel_name matches expected format: private-user-{user_id} (403 if mismatch)
- Uses get_pusher_client() to retrieve Pusher client instance
- Handles Pusher not configured gracefully (503 Service Unavailable)
- Calls pusher_client.authenticate(channel, socket_id) to generate auth signature
- Returns Pusher auth response directly (includes 'auth' and 'channel_data' fields)
- Wraps authentication in try/except for exception handling (500 on error)
- Created comprehensive test suite with 8 test cases in test_pusher_auth.py:
  - Successful authentication with correct channel
  - Reject wrong channel (different user)
  - Require channel_name parameter
  - Require socket_id parameter
  - Handle Pusher not configured
  - Handle Pusher authentication exception
  - Require authentication (401 for anonymous)
  - Reject public channels
- All tests pass (8/8), mypy typecheck passes
- Commit 4b5d095
- Files changed:
  - backend/apps/notifications/views.py (added pusher_auth action)
  - backend/apps/notifications/test_pusher_auth.py (new test file)
  - PRD19.md (marked US-019 complete)
- Learnings for future iterations:
  - Pusher private channels require POST to auth endpoint with channel_name and socket_id
  - pusher_client.authenticate() returns dict with 'auth' signature and 'channel_data'
  - Channel validation prevents users from subscribing to other users' channels
  - Service unavailable (503) is appropriate for missing external service configuration
  - Internal server error (500) for unexpected exceptions during external API calls
  - Mock get_pusher_client in tests to avoid requiring actual Pusher credentials
  - @action(detail=False, methods=['post']) for non-resource-specific POST endpoints
---

## Iteration 20 - US-020: Create Email Notification Service
- Created apps/notifications/email.py with send_notification_email() function
- Implemented TYPE_CHECKING pattern to fix mypy type annotation issues with User model
- Function takes user and list of notifications, returns boolean for success
- Returns False immediately for empty notification lists (no email sent)
- Generates both HTML and plain text email versions using Django templates
- Created email_digest.html template with Bitcoin DeFi aesthetic:
  - Gradient orange header (#EA580C to #F7931A)
  - Dark background (#030304, #0F1115)
  - Notification items with left orange border accent
  - Mobile-responsive design with max-width container
  - Call-to-action button with gradient background
  - Footer with preferences link
- Created email_digest.txt template for plain text fallback
- Subject line logic: single notification shows title, multiple shows count
- EmailMultiAlternatives for both HTML and plain text in one message
- Graceful error handling with try/except (logs but doesn't raise)
- Created comprehensive test suite with 6 test cases:
  - Single notification email format
  - Multiple notifications email format
  - Empty list returns False
  - Both HTML and plain text versions included
  - Correct subject line for single notification
  - Correct subject line for multiple notifications
- All tests pass (6/6), mypy typecheck passes
- Commit 7dffeb1
- Files changed:
  - backend/apps/notifications/email.py (new)
  - backend/templates/notifications/email_digest.html (new)
  - backend/templates/notifications/email_digest.txt (new)
  - backend/apps/notifications/test_email.py (new)
  - PRD19.md (marked US-020 complete)
- Learnings for future iterations:
  - Use TYPE_CHECKING pattern for Django User model type annotations:
    ```python
    from typing import TYPE_CHECKING
    if TYPE_CHECKING:
        from django.contrib.auth.models import User
    else:
        User = get_user_model()
    ```
  - Then use quoted string in function signature: def func(user: "User")
  - EmailMultiAlternatives for sending HTML + plain text emails
  - attach_alternative(html_content, "text/html") adds HTML version
  - Django templates in backend/templates/ directory
  - render_to_string() for rendering templates to strings
  - Email tests use Django's mail.outbox to inspect sent emails
  - Test both email.body (plain text) and email.alternatives[0] (HTML)
  - Fail gracefully in email sending (return False, don't raise exception)
  - Log errors with print() for debugging (could use Django logging in production)
---

## Iteration 21 - US-022: Celery Task for Immediate Email Notifications
- Added emailed_at field to Notification model (nullable datetime)
- Generated and applied migration 0004_notification_emailed_at
- Created apps/notifications/tasks.py with send_immediate_notification_email Celery task
- Task checks user's email_digest_frequency preference before sending
- Only sends email if preference is 'immediate', returns False otherwise
- Marks notification as emailed (sets emailed_at timestamp) after successful send
- Integrated task into create_notification service with .delay() for async execution
- Created comprehensive test suite with 7 test cases:
  - Sends email when preference is immediate
  - Skips email when preference is daily/weekly/none
  - Handles missing notification gracefully
  - Handles missing preferences gracefully
  - Only marks as emailed on successful send (not on failure)
- All tests pass (7/7), mypy typecheck passes
- Commit 4d7c22b
- Files changed:
  - backend/apps/notifications/models.py (added emailed_at field)
  - backend/apps/notifications/migrations/0004_notification_emailed_at.py (new)
  - backend/apps/notifications/tasks.py (new)
  - backend/apps/notifications/services.py (integrated task call)
  - backend/apps/notifications/test_immediate_email_task.py (new)
  - PRD19.md (marked US-022 complete)
- Learnings for future iterations:
  - Celery shared_task decorator needs type: ignore[import-untyped]
  - Task functions should handle missing objects gracefully (DoesNotExist)
  - Check preferences via try/except to handle missing NotificationPreferences
  - .delay() queues task asynchronously, needs type: ignore[attr-defined] for mypy
  - select_related for efficient query when accessing user and preferences
  - Update specific fields with save(update_fields=['field']) for efficiency
  - notification.id needs type: ignore[attr-defined] throughout tests
  - Integration point: create_notification() calls task after creating notification
  - Task filtering: only send if frequency == 'immediate', otherwise return False
---

## Iteration 22 - US-023: Celery Beat Task for Daily/Weekly Email Digests
- Created send_daily_digest() and send_weekly_digest() tasks in apps/notifications/tasks.py
- Daily task runs at 9 AM UTC, weekly runs Monday 9 AM UTC (configured in config/celery.py)
- Both tasks query users with matching email_digest_frequency preference
- Tasks fetch un-emailed notifications using filter(emailed_at__isnull=True)
- Batch all un-emailed notifications per user into single email via send_notification_email()
- Mark all notifications as emailed using bulk update() after successful send
- Returns count of users who received emails for monitoring
- Added tasks to Celery beat schedule in config/celery.py
- Created comprehensive test suite with 13 test cases:
  - DailyDigestTaskTestCase (7 tests): sends to daily users, batches notifications, marks as emailed, skips already emailed, skips users with no notifications, ignores other frequencies, returns zero when no users
  - WeeklyDigestTaskTestCase (6 tests): sends to weekly users, batches notifications, marks as emailed, skips already emailed, ignores other frequencies, returns zero when no users
- All tests pass (13/13), mypy typecheck passes
- Commit 7b7edcd
- Files changed:
  - backend/apps/notifications/tasks.py (added digest tasks)
  - backend/config/celery.py (added beat schedule entries)
  - backend/apps/notifications/test_digest_tasks.py (new test file)
  - PRD19.md (marked US-023 complete)
- Learnings for future iterations:
  - Import models locally in task functions to avoid circular imports
  - NotificationPreferences.objects.filter(email_digest_frequency='daily').select_related('user') for efficient query
  - Use .exists() to check if queryset has results before processing
  - Bulk update pattern: get list of IDs, then .filter(id__in=ids).update(field=value)
  - list() conversion needed for queryset before passing to send_notification_email
  - Celery beat schedule uses crontab(hour=9, minute=0) for daily, add day_of_week=1 for Monday
  - Return count of emails sent for monitoring/logging purposes
  - Print statements useful for debugging Celery task execution
---

## Iteration 23 - US-024: Celery Task for Notification Cleanup
- Implemented cleanup_old_notifications() task in apps/notifications/tasks.py
- Deletes read notifications older than 7 days using filter(read_at__isnull=False, read_at__lt=seven_days_ago).delete()
- Deletes unread notifications older than 30 days using filter(read_at__isnull=True, created_at__lt=thirty_days_ago).delete()
- Returns dict with read_deleted, unread_deleted, and total counts for monitoring
- Added to Celery beat schedule in config/celery.py (runs daily at 2:00 AM UTC)
- Created comprehensive test suite with 7 test cases:
  - Deletes read notifications older than 7 days
  - Deletes unread notifications older than 30 days
  - Keeps unread notifications under 30 days
  - Keeps read notifications under 7 days
  - Logs correct deletion counts
  - Handles empty database gracefully
  - Mixed notifications cleanup (old and recent)
- All tests pass (7/7), mypy typecheck passes
- Commit 37caecd
- Files changed:
  - backend/apps/notifications/tasks.py (added cleanup_old_notifications task)
  - backend/config/celery.py (added beat schedule entry)
  - backend/apps/notifications/test_cleanup_task.py (new test file)
  - PRD19.md (marked US-024 complete)
- Learnings for future iterations:
  - auto_now_add=True fields cannot be overridden in create(), must use update() after creation
  - Use Notification.objects.filter(id=obj.id).update(created_at=...) to bypass auto_now_add
  - timedelta needs to be imported from datetime module
  - .delete() returns tuple (count, {model: count_dict}), use first element for total count
  - Celery beat schedule uses crontab(hour=X, minute=Y) for daily tasks
  - Return dict from cleanup tasks for monitoring/logging purposes
  - Schedule cleanup tasks during off-peak hours (2 AM UTC)
---

## Iteration 24 - US-025: Add Notification Types and API Client to Frontend
- Created frontend/src/types/notifications.ts with all TypeScript interfaces
- Defined NotificationType union type with 5 values (vault_invite, member_joined, source_added, annotation_reply, mention)
- Defined EmailDigestFrequency type with 4 values (immediate, daily, weekly, none)
- Created Notification interface with all fields including is_read boolean
- Created NotificationPreferences interface matching backend model
- Created MutedVault interface with vault_id and vault_name for display
- Added UnreadCountResponse and MarkAllReadResponse helper interfaces
- Implemented 9 notification API functions in frontend/src/lib/api.ts:
  - getNotifications(unreadOnly, page) with pagination support
  - markAsRead(id) for single notification
  - markAllAsRead() for bulk read operation
  - getUnreadCount() for badge display
  - getPreferences() and updatePreferences(data) for settings
  - getMutedVaults(), muteVault(vaultId), unmuteVault(vaultId)
- All API functions properly typed with request/response types
- TypeScript typecheck passes without errors
- Commit df7dd8c
- Files changed:
  - frontend/src/types/notifications.ts (new)
  - frontend/src/lib/api.ts (added notification API functions)
  - PRD19.md (marked US-025 complete)
- Learnings for future iterations:
  - Union types in TypeScript use pipe operator: 'value1' | 'value2' | 'value3'
  - Record<string, any> for flexible JSON data fields from backend
  - Nullable fields use type | null syntax
  - URLSearchParams for building query strings with optional parameters
  - Paginated responses include count, next, previous, results fields
  - PATCH for partial updates, POST for actions like mark-all-read
  - Import types from @/types/ path, not relative paths
  - Export types and interfaces for use in components and hooks
---

## Iteration 25 - US-026: Create NotificationBell Component with Badge
- Created frontend/src/components/features/notifications/NotificationBell.tsx
- Component accepts unreadCount and onClick props
- Bell icon from lucide-react with hover states
- Badge displays unread count with "9+" for counts > 9
- Badge hidden when count is 0
- Badge styled with Bitcoin DeFi gradient (EA580C to F7931A)
- Positioned absolutely on top-right of bell icon
- Responsive button with hover effect (bg-white/5)
- Removed unused useState import to fix typecheck error
- TypeScript typecheck passes
- Files changed:
  - frontend/src/components/features/notifications/NotificationBell.tsx (new)
  - PRD19.md (marked US-026 complete except browser verification)
- Learnings for future iterations:
  - Remove unused imports immediately to avoid typecheck errors
  - Use 'use client' directive for client-side interactive components in Next.js
  - Badge positioning: absolute with negative top/right offsets
  - Display "9+" for counts > 9 using ternary operator
  - Conditional rendering with && operator for showing/hiding badge
  - Min-width on badge ensures single digits are circular
  - Accessibility: add aria-label with current unread count
  - Gradient classes match project design system (#EA580C to #F7931A)
---

## Iteration 26 - US-027: Create NotificationItem Component
- Created frontend/src/components/features/notifications/NotificationItem.tsx
- Implemented icon mapping for 5 notification types using lucide-react icons:
  - vault_invite: Bell
  - member_joined: UserPlus
  - source_added: FileText
  - annotation_reply: MessageCircle
  - mention: AtSign
- All icons colored with Bitcoin orange (#F7931A)
- Implemented formatRelativeTime utility:
  - just now (< 1 min)
  - X minutes ago (< 1 hour)
  - X hours ago (< 24 hours)
  - X days ago (< 7 days)
  - Full date for older notifications
- Implemented truncateText utility (max 100 chars with ellipsis)
- Component displays:
  - Icon on left
  - Title in bold white text
  - Body preview in muted gray
  - Relative timestamp
  - Orange dot indicator for unread (gradient border)
  - Unread items have subtle orange background tint (bg-[#F7931A]/5)
- Hover state with bg-white/5 transition
- Border-bottom separator between items
- TypeScript typecheck passes
- Commit 1c60b75
- Files changed:
  - frontend/src/components/features/notifications/NotificationItem.tsx (new)
  - PRD19.md (marked US-027 complete except browser verification)
- Learnings for future iterations:
  - Switch statement for mapping enum values to UI elements
  - Relative time calculation: subtract timestamps, convert to secs/mins/hours/days
  - Use Math.floor() for rounding down time differences
  - Optional onClick callback with ?. operator for safe invocation
  - flex-shrink-0 prevents icon from being compressed
  - min-w-0 on flex child enables text truncation with ellipsis
  - Unread styling: combine subtle background tint + dot indicator
  - Border separator on items inside dropdown lists
---

## Iteration 27 - US-028: Create NotificationDropdown Component
- Created frontend/src/components/features/notifications/NotificationDropdown.tsx
- Implemented dropdown panel with controlled open/close state via isOpen prop
- Header displays "Notifications" title with "Mark all as read" button (only shows if notifications exist)
- Displays max 10 most recent notifications using NotificationItem component
- Scrollable list with max-height of 500px for overflow
- Empty state message: "No notifications yet" (centered, muted text)
- Footer with "View all notifications" link (only shows if notifications exist)
- Click outside to close using useRef + useEffect pattern
- Click on notification calls onNotificationClick and auto-closes dropdown
- Dark card styling: bg-[#0F1115], border white/10, orange shadow glow
- Responsive max-width: max-w-[calc(100vw-2rem)] for mobile
- TypeScript typecheck passes
- Commit fc070b9
- Files changed:
  - frontend/src/components/features/notifications/NotificationDropdown.tsx (new)
  - PRD19.md (marked US-028 complete except browser verification)
- Learnings for future iterations:
  - useRef<HTMLDivElement> for tracking dropdown container element
  - useEffect cleanup pattern for event listeners (add on mount, remove on unmount)
  - setTimeout delay on addEventListener prevents immediate click-outside firing
  - event.stopPropagation() on buttons inside dropdown prevents closing
  - .slice(0, 10) limits array to first 10 items
  - Conditional rendering: show header button and footer only if notifications exist
  - absolute + right-0 + top-full + mt-2 positions dropdown below parent
  - z-50 ensures dropdown appears above other content
  - overflow-hidden on container, overflow-y-auto on scrollable section
  - Call onClose() after navigation to dismiss dropdown
---

## Iteration 28 - US-029: Create useNotifications Hook (Verification)
- Task was already completed in previous session
- Verified frontend/src/hooks/useNotifications.ts exists with comprehensive implementation
- Hook exceeds acceptance criteria with additional features:
  - Pagination support with page, limit, and isRead filter options
  - Polling for unread count with configurable interval (default 60s)
  - Document visibility tracking to pause polling when tab hidden
  - Automatic cache updates on mutations
  - Error handling for both notifications list and unread count
  - Refetch functions for manual updates
  - Mutation pending states (isMarkingAsRead, isMarkingAllAsRead)
- All acceptance criteria met:
  - Uses React Query (useQuery, useMutation)
  - Fetches notifications with pagination
  - Fetches unread count with polling
  - markAsRead(id) mutation with cache updates
  - markAllAsRead() mutation with cache invalidation
  - Refetch on window focus via polling when visible
- TypeScript typecheck passes
- Commit 94a5be8
- Files: frontend/src/hooks/useNotifications.ts, PRD19.md
- Learnings for future iterations:
  - Always verify existing implementation before recreating
  - Use document.visibilitychange event to track tab visibility
  - refetchInterval can be a function returning interval or false for conditional polling
  - refetchIntervalInBackground: false prevents polling when tab is hidden
  - setQueryData with updater function for optimistic cache updates
  - invalidateQueries for full refetch when data structure changes significantly
  - Mark notification as complete if implementation exceeds requirements
---

## Iteration 29 - US-030: Integrate NotificationBell into App Header (Verification)
- Task was already completed in previous session
- Verified AppHeader component (frontend/src/components/features/notifications/AppHeader.tsx) already integrates NotificationBell
- Bell positioned at line 133-149, before user profile menu (lines 152-194)
- Notification bell uses useNotifications() hook to fetch unread count on mount (line 34)
- Only shown when authenticated (app layout validates auth before rendering header)
- TypeScript typecheck passes
- All acceptance criteria met:
  - NotificationBell in app header (lines 133-149)
  - Positioned before user profile menu
  - Only shown when authenticated (layout-level auth check)
  - Fetches unread count on mount via useNotifications hook
  - Typecheck passes
- Files: frontend/src/components/features/notifications/AppHeader.tsx, PRD19.md
- Learnings for future iterations:
  - Always verify existing implementations before assuming task needs work
  - AppHeader component handles notification bell, unread badge, dropdown panel, and click-outside logic
  - useNotifications hook provides unreadCount state that updates automatically
  - App layout ensures header only renders for authenticated users
  - NotificationPanel component handles dropdown display (separate from bell)
  - Bell button uses ref for click-outside detection (bellButtonRef)
---

## Iteration 30 - US-031: Set up Pusher Client for Real-time Notifications
- Updated existing frontend/src/lib/pusher.ts to match US-031 requirements
- Installed pusher-js package (already installed, verified up to date)
- Implemented custom authorizer for private channel authentication:
  - Uses POST /api/v1/notifications/pusher/auth/ endpoint
  - Sends channel_name and socket_id in request body
  - Receives auth signature from backend
  - Properly typed with ChannelAuthorizationData interface
- Changed from auth.headers pattern to authorizer callback pattern for proper Pusher authentication
- Added ChannelAuthorizationData interface definition (not exported by pusher-js)
- Preserved requestNotificationPermission() function for backward compatibility (used by NotificationPreferences)
- Exported getPusherClient() as default export and named export
- Helper functions: subscribeToUserChannel(), unsubscribeFromUserChannel(), disconnectPusher()
- TypeScript typecheck passes without errors
- Commit 057d572
- Files changed:
  - frontend/src/lib/pusher.ts (updated authorizer, types, imports)
  - PRD19.md (marked US-031 complete)
- Learnings for future iterations:
  - Pusher authorizer callback must match exact signature: (error: Error | null, authData: ChannelAuthorizationData | null) => void
  - Don't use async/await in authorizer, use .then()/.catch() instead for Promise handling
  - ChannelAuthorizationData interface has fields: auth (required), channel_data (optional), shared_secret (optional)
  - Define local interfaces for types not exported by external libraries
  - Check for existing implementations and preserve backward-compatible functions
  - Custom authorizer pattern preferred over authEndpoint + headers for more control
  - Import api client from @/lib/api to make authenticated requests
---
## Iteration 31 - US-032: Create usePusherNotifications Hook for Real-time
- Created frontend/src/hooks/usePusherNotifications.ts
- Hook accepts userId and enabled options via UsePusherNotificationsOptions interface
- Subscribes to private-user-{userId} channel using getPusherClient()
- Listens for 'notification' event and updates React Query cache:
  - Prepends new notification to existing results array
  - Increments count in paginated data structure
  - Initializes cache if empty with first notification
- Listens for 'badge_update' event and updates unread count cache directly
- Uses setQueryData with updater function for cache manipulation
- Cleanup: unbind_all() and unsubscribe() on component unmount
- Handles missing Pusher client gracefully with console.warn
- Error handling for subscription failures with try/catch
- TypeScript typecheck passes
- Commit c707cd3
- Files changed:
  - frontend/src/hooks/usePusherNotifications.ts (new)
  - PRD19.md (marked US-032 complete)
- Learnings for future iterations:
  - Import Channel type from pusher-js for proper typing
  - Use useEffect with userId and enabled as dependencies for reactive subscription
  - setQueryData updater function receives oldData and returns new data
  - Prepend notifications with [notification, ...oldData.results] for chronological order
  - Query key for unread count is ['notifications', 'unread-count'] (array format)
  - channel.bind() for event listeners, channel.unbind_all() before unsubscribe
  - pusher.subscribe() returns Channel object, store in state for cleanup
  - Return cleanup function from useEffect to unbind/unsubscribe
  - Handle undefined oldData case when cache is empty
---

## Iteration 32 - US-033: Create Toast Notification System
- Created NotificationToast component with full toast popup functionality
- Implemented auto-dismiss timer (5 seconds) with cleanup on unmount
- Exit animation using isExiting state and setTimeout for smooth slide-out
- Click handler navigates and dismisses toast via onNavigate callback
- Dismiss button (X) uses stopPropagation to prevent click-through
- Icon mapping using typeIcons record (same pattern as NotificationItem)
- Truncate body text to 100 characters with truncateText utility
- Created NotificationToastContainer for managing multiple toasts
- Container stacks up to 3 toasts using .slice(0, 3)
- Toasts stack with newest at bottom using flex-col-reverse
- Slide-in/out animation: translate-x-full (exit) to translate-x-0 (enter)
- Positioned fixed bottom-right with z-50 for overlay
- pointer-events-none on container, pointer-events-auto on individual toasts
- Bitcoin DeFi styling: bg-[#0F1115], border white/10, orange shadow glow
- Hover effects: border-[#F7931A]/50 and -translate-y-1 lift
- Accessibility: role="alert" and aria-live="polite" for screen readers
- TypeScript typecheck passes
- Commit 1bbdf79
- Files changed:
  - frontend/src/components/features/notifications/NotificationToast.tsx (new)
  - PRD19.md (marked US-033 complete except browser verification)
- Learnings for future iterations:
  - Use state + setTimeout for controlled exit animations before unmount
  - Match animation duration in code (setTimeout) with CSS transition-duration
  - stopPropagation on nested buttons prevents parent click handlers
  - flex-col-reverse reverses visual order (newest at bottom in DOM = bottom on screen)
  - pointer-events-none on container + pointer-events-auto on children for click-through
  - Auto-dismiss pattern: useEffect with timer, cleanup on unmount
  - role="alert" + aria-live="polite" for accessibility announcements
  - Fixed positioning with z-50 ensures toasts appear above all content
---


## Iteration 33 - US-034: Integrate Toast Notifications with Pusher
- Extended usePusherNotifications hook to accept optional onNotification callback
- Integrated NotificationToastContainer into app layout (frontend/src/app/(app)/layout.tsx)
- Added state management for toast notifications (add, dismiss, navigate)
- Implemented conditional toast display logic:
  - Don't show if notification panel is open (tracked via window custom events)
  - Don't show if page is hidden (document.hidden check)
- Updated AppHeader to emit 'notification-panel-opened' and 'notification-panel-closed' events
  - Emits on toggle, click outside, and Escape key
- Created navigation handler that maps notification types to routes:
  - vault_invite → /vaults/{vault_id}
  - member_joined → /vaults/{vault_id}/members
  - source_added → /vaults/{vault_id}/sources/{source_id}
  - annotation_reply → /vaults/{vault_id}/sources/{source_id}#annotation-{annotation_id}
  - mention → same as annotation_reply
- Toast callback triggered in usePusherNotifications after cache update
- All acceptance criteria met, TypeScript typecheck passes
- Commit 810cc7b
- Files changed:
  - frontend/src/app/(app)/layout.tsx (added toast integration)
  - frontend/src/hooks/usePusherNotifications.ts (added onNotification callback)
  - frontend/src/components/features/notifications/AppHeader.tsx (emit panel state events)
  - PRD19.md (marked US-034 complete except browser verification)
- Learnings for future iterations:
  - Window custom events pattern for cross-component state tracking
  - CustomEvent API: new CustomEvent(name) and window.dispatchEvent(event)
  - Listen with window.addEventListener() and cleanup with removeEventListener()
  - Check document.hidden to detect if page is visible/hidden (tab switching)
  - Notification type to route mapping via Record<string, string> dictionary
  - Optional callback in hooks enables flexible integration patterns
  - Add callback to useEffect dependencies to avoid stale closures
  - State tracking via events decouples components (no prop drilling)
---

## Iteration 34 - US-035: Implement Notification Click Navigation
- Implemented route mapping in NotificationPanel.tsx with getNotificationRoute() function
- Maps 5 notification types to specific routes:
  - vault_invite → /vaults/{vault_id}
  - member_joined → /vaults/{vault_id}/members
  - source_added → /vaults/{vault_id}/sources/{source_id}
  - annotation_reply → /vaults/{vault_id}/sources/{source_id}#annotation-{annotation_id}
  - mention → same as annotation_reply (with anchor link)
- Updated backend signals to include vault_id in annotation_reply and mention notifications
  - annotation_reply data now has: vault_id, source_id, annotation_id, parent_id, replier_name, preview
  - mention data now has: vault_id, source_id, annotation_id, mentioner_name, preview
- handleNotificationClick marks as read and navigates to route
- Dropdown auto-closes after navigation (already implemented in existing code)
- Fixed Notification type conflicts between @/lib/api/notifications and @/types/notifications
  - Updated @/lib/api/notifications to re-export type from @/types/notifications
  - Changed NotificationPanel import to use correct Notification type
  - Updated API function markAsRead to accept string | number for ID compatibility
  - Changed display from notification.message to notification.title
- Ran backend tests: 6 annotation signal tests + 7 mention signal tests all pass
- TypeScript typecheck passes with no errors
- Commit c91e4be
- Files changed:
  - backend/apps/notifications/signals.py (added vault_id to annotation data)
  - frontend/src/components/features/notifications/NotificationPanel.tsx (added navigation logic)
  - frontend/src/lib/api/notifications.ts (fixed type imports and ID type)
  - PRD19.md (marked US-035 complete except browser verification)
  - progress19.txt (this entry)
- Learnings for future iterations:
  - Always check for type conflicts when multiple files define similar types
  - Use single source of truth for types by re-exporting from canonical location
  - Optional chaining (?.) and null checks essential for potentially undefined data fields
  - Backend signal data structure must include all IDs needed for frontend routing
  - Source.vault.id provides vault_id for annotation-related notifications
  - Router.push() from next/navigation for client-side navigation
  - Switch statement pattern clean for mapping enum values to routes
  - Navigation happens after marking as read for better UX (no wait)
  - Test backend changes with existing test suites before frontend integration
---
## Iteration 35 - US-036: Create Notification Preferences Page Layout
- Created dedicated notification preferences page at frontend/src/app/(app)/settings/notifications/page.tsx
- Implemented three main sections with Bitcoin DeFi card-based layout:
  - Email Notifications section (placeholder for US-037, US-038)
  - Push Notifications section (placeholder for US-039)
  - Muted Vaults section with API integration
- Added loading skeleton with animate-pulse for better UX
- Back link to settings page with ChevronLeft icon
- Fetches muted vaults from API using React Query
- Displays muted vaults list or empty state with icon
- Gradient header matching app design system
- TypeScript typecheck passes
- Commit a0d6da2
- Files changed:
  - frontend/src/app/(app)/settings/notifications/page.tsx (new)
  - PRD19.md (marked US-036 complete except browser verification)
- Learnings for future iterations:
  - Import getMutedVaults from @/lib/api, not @/lib/api/notifications
  - MutedVault type is in @/types/notifications
  - Removed unused useState and preferences query (will be added in US-037)
  - Card sections follow consistent pattern: icon circle + title + subtitle + content
  - Loading skeleton mirrors actual content structure for smooth transition
  - Placeholder text for future US tasks prevents "coming soon" repetition
  - Back navigation using Link from next/link for client-side routing
  - Empty state includes icon, message, and helpful hint for users
---

## Iteration 36 - US-037: Add Email Notification Toggles to Preferences
- Implemented toggle switches for "Vault activity" and "Mentions" email notifications
- Added React Query mutations for updating preferences with optimistic updates
- Optimistic update pattern: save snapshot, update cache, rollback on error, refetch on settled
- Added "Saved" confirmation indicator with Check icon (auto-dismiss after 2 seconds)
- Toggle switches styled with Bitcoin DeFi gradient (EA580C to F7931A) when enabled
- Dark background (#1E293B) when disabled for clear visual state
- Toggle animation using transform translate-x with smooth transition
- Hover effects on preference rows (border-[#F7931A]/20)
- Toggle state controlled by preferences?.field_name with disabled state when preferences loading
- handleToggle function accepts field name and new value, calls mutation
- TypeScript typecheck passes
- Commit 9256744
- Files changed:
  - frontend/src/app/(app)/settings/notifications/page.tsx (added toggles, mutations, state)
  - PRD19.md (marked US-037 complete except browser verification)
- Learnings for future iterations:
  - useMutation with onMutate for optimistic updates: cancel queries, snapshot, update cache
  - onError receives context from onMutate to rollback changes
  - onSettled always runs (success or error), perfect for invalidateQueries
  - setQueryData with updater function: (old) => ({ ...old, ...new })
  - Show temporary UI feedback with useState + setTimeout pattern
  - Disabled attribute prevents interaction while data is loading
  - Toggle button pattern: relative container with absolute positioned toggle circle
  - Transform translate-x-1 (off) vs translate-x-6 (on) for toggle animation
  - Gradient background only on enabled state, solid dark color for disabled
  - Check icon from lucide-react for "Saved" confirmation
---

